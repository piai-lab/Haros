---
type: "Decision"
title: "B1 appSettings boundary-repair PASS approval"
---

# B1 appSettings boundary-repair PASS approval

The fresh different-actor [scoped QbD 2 audit](../qbd/b1-appsettings-boundary-repair-audit.md)
returned `PASS` with zero blockers and zero advisories. Under the maintainer's explicit standing
instruction to repair exact implementation blockers and continue without routine pauses, option 1
is selected: restart B1 using the repaired eleven-path boundary and a fresh implementation
receipt, preserving the current uncommitted Service work from the stopped receipt.

Only `apps/web/src/appSettings.ts` and `apps/web/src/appSettings.test.ts` are newly authorized, and
only to remove the retired `enableAppshots` donor input/test while preserving `enableAppSnap` as
the sole current key. The failed `direct_first_public_b1_i2` receipt remains failed and cannot be
reused. The frozen complexity instrument at
`45df49a6afde882d32c1dcd00457c7787d227e4a`, all destructive exclusions, seven B1 done
conditions, A1-A15 coverage and the literal accepted-Work sequence remain unchanged. Any further
required path or unclassified compatibility occurrence again stops for map repair.
