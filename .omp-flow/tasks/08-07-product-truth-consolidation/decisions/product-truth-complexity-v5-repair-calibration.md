---
type: "Decision"
title: "Product-truth complexity v5 repair calibration"
---

# Product-truth complexity v5 repair calibration

## Human calibration applied

Under the maintainer's standing instruction to continue the fastest high-quality path without
weakening Product truth or destructive safety, Main accepts the independent
[v4 meter Review](../reviews/product-truth-complexity-v4.md) as `FAIL` and selects the narrow repair
path. Immutable v4 commit `70eb77da3512f5aa91b420302623bc91efa56f0b` and its handoff remain
rejected evidence and must not be amended or relabelled.

The replacement measurement version must close all three P0 groups:

1. candidate-time persistence capability discovery includes every frozen Work production member,
   including paths classified separately as direct-rebuild tool. Direct-tool lock handles remain
   excluded only from ordinary-runtime owner-lock proof, never from opener/path/handle discovery;
2. each successful lock acquisition creates a unique abstract handle/token. Branch joins retain
   `held` only when every predecessor carries the same acquisition identity and binding. Detached
   or unresolved callback/microtask release is an unknown/release-capable edge and cannot certify a
   later sink;
3. refusal proof uses bounded CFG/ICFG reachability, not source positions or token presence. It
   models zero-iteration loops, constant-false/unreachable throws, catch/finally continuation and
   present/absent successor truth. Unsupported flow fails closed.

Mandatory single-cause negatives include the Review's exact direct-tool raw Product SQLite/DELETE,
different-handle branch phi, detached microtask release, zero-iteration `for` guard and conditional
false rethrow that swallows the reset. Inherited v4 authority, dependency, provenance, polarity,
must-hold and hidden composition fixtures remain conjunctive.

This decision changes measurement authority only. It does not authorize v5 implementation before
a fresh different-actor zero-finding QbD, B1 production work, destructive execution, real
`~/.omnimind` access or any protected-exclusion relaxation.
