---
type: "Decision"
title: "Product-truth complexity v8 QbD PASS approval"
---

# Product-truth complexity v8 QbD PASS approval

The fresh different-actor [v8 r4 QbD audit](../qbd/product-truth-complexity-v8-audit.md) returned
`PASS` with zero blockers and zero advisories on immutable authority checkpoint
`23b309b0da3ae65a7809002090a539f6c7ee7c51`.

Under the maintainer's standing direction to repair exact blockers and continue without routine
pauses, option 1 is selected: authorize only the bounded
[product-truth complexity v8 measurement Work](../work/product-truth-complexity-v8.md).

The official invocation must supply exactly one Main/human-selected full evidence commit through
`--predecessor-evidence`. Candidate, config, repository files, report prose, receipt values and Git
history may neither select nor override it. The meter proves only content consistency relative to
that selected trust root and records the deterministic evidence tuple; it does not authenticate a
reviewer or human. A later different actor must compare the complete official invocation, selected
SHA, handoff and tuple with the linked
[trust-root calibration](product-truth-complexity-v8-trust-root-calibration.md).

All v1-v7 bytes remain immutable. The repaired qualified declaration/site rules, outside-Work
byte/import/ingress equality, no-CFG boundary and five exact Product Work fences remain unchanged;
B1's sole boundary addition remains `scripts/release-smoke.ts`.

This approval does not authorize B1, production changes, destructive execution, real
`~/.omnimind` access, Harness/schema changes or any downstream Product Work. V8 must first be
implemented, frozen at one immutable SHA, and accepted by a zero-finding different-actor Review of
its code, official invocation and evidence tuple. Any mismatch or further authority/path need stops
for a new bounded repair.
