---
type: "Decision"
title: "Product-truth complexity v3 PASS approval"
---

# Product-truth complexity v3 PASS approval

## Human decision applied

The maintainer's standing direction is to continue the selected option-1 repair aggressively and
without pausing for ordinary implementation choices, while preserving the exact protected-data and
default-`~/.omnimind` boundaries. Applied to the
[zero-finding v3 audit](../qbd/product-truth-complexity-v3-pass-audit.md), that direction accepts its
`PASS` with zero blockers and zero advisories.

Only the measurement-only [v3 meter Work](../work/product-truth-complexity-v3.md) is authorized next.
The accepted immutable Design/docs checkpoint SHA must be named in the assignment and pinned by the
meter. The Work may create only its listed v3 meter, config, focused fixtures/tests and handoff; it
may not change product runtime, destructive behavior, dependency manifests, v1/v2 evidence, user
state or the direct rebuild tool.

This approval does not accept the rejected B1 candidate, authorize destructive execution, promote
Campaign claims, or authorize any later product Work. A dedicated immutable v3 meter commit and a
different-actor Review `PASS` remain mandatory before a new B1 production receipt may be issued.
