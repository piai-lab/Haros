---
type: "Decision"
title: "Product-truth complexity v7 final QbD repair calibration"
---

# Product-truth complexity v7 final QbD repair calibration

## Human calibration applied

Main accepts the independent [v7 final authority audit](../qbd/product-truth-complexity-v7-final-audit.md)
as `FAIL` under the maintainer's standing instruction to continue the smallest closed repair. The
v7 Occam split, Product/destructive scope, operation universe and five Work fences remain selected.

The raw-effect grammar must use one exact source-form vocabulary. It must normalize or reject every
standard global alias (`globalThis`, `global`, `self`, `window`) before selector classification,
cover both literal and non-literal computed members with distinct machine identities, and fail any
unresolved alias or selector. Meter/config code may not infer additional forms.

The B1 verifier universe must also freeze its input universe before implementation: exact
Design-owned fixture-state IDs and definitions, per-owner applicable state IDs, resource/chunk/key
cardinalities, ordinal derivation, race/kill convergence states and canonical fixture digests. A
future generator may materialize those definitions but may not add, omit, merge, rename or shrink
them; manifest completeness is checked against the frozen cartesian product.

This decision authorizes only that bounded v7 authority repair and one fresh different-actor QbD.
It authorizes no meter implementation, B1 production work, destructive execution, dependency
change, Campaign promotion or read/mutation of real `~/.omnimind`.
