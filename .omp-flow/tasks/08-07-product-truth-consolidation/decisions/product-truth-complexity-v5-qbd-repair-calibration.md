---
type: "Decision"
title: "Product-truth complexity v5 QbD repair calibration"
---

# Product-truth complexity v5 QbD repair calibration

## Human calibration applied

Main accepts the independent [v5 authority audit](../qbd/product-truth-complexity-v5-audit.md) as
`FAIL` under the maintainer's standing instruction to continue the narrow high-quality repair. The
selected option is to close the missing direct-tool classifier-copy origin without weakening
capability analysis, destructive safety or the five Work fences.

V5 must add one strict, independently extracted path-origin authority for the future
`scripts/product-truth/sqlite-classifier.ts` private copy. The origin is valid only when it derives
from an exact retired tool target, is contained below the invocation-owned freshly created private
scratch directory, preserves no-follow/copy identity binding, is opened read-only, and is removed on
every normal/throw/finally exit. It is a distinct
`ephemeral-direct-tool-classifier-copy` class, not a general temp or tool exemption.

The class must never discharge current `stores/product.sqlite` or `stores/service.sqlite`, a source
database opened in place, an unbound temp path, a raw current Product alias or a copy without complete
cleanup. Mandatory fixtures cover the valid private-copy classifier plus each of those negatives,
while retaining the failed-v4 direct-tool raw Product SQLite/DELETE case.

This decision changes measurement authority only. It authorizes no v5 implementation until a fresh
different-actor QbD returns zero blocker and zero advisory, no B1 production work, no destructive
execution and no real `~/.omnimind` access.
