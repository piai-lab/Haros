---
type: "Decision"
title: "Product-truth complexity v4 QbD repair calibration"
---

# Product-truth complexity v4 QbD repair calibration

## Human calibration applied

Consistent with the maintainer's standing direction to continue the fastest high-quality repair
without weakening Product truth or destructive safety, Main accepts the independent
[v4 authority audit](../qbd/product-truth-complexity-v4-audit.md) as `FAIL` and selects option 1:
repair the current measurement authority.

The repair remains measurement-only and must add both missing machine boundaries:

1. a candidate-independent database-capability inventory, re-derived from the accepted immutable
   source/dependency graph, covering every reachable repository/external persistent opener,
   constructor, layer/factory and handle receiver. `proved non-Product` is limited to exact approved
   path-authority origins such as canonical service or proved nonpersistent memory/temp sources;
   omission, mutation, new unresolved persistence capability or unknown persistent flow fails;
2. a canonical owner-lock must-hold model. Exact acquire/release declarations, handle aliases,
   lane/root/current-database binding and all normal/throw/catch/finally exits are resolved. The
   definitely-held fact must dominate the post-lock cut and every assigned current sink, and no
   release may precede completion of that region.

Mandatory fixtures include real Effect/service-layer inventory coverage and per-seed
omit/mutation, raw Product path through the same capability, plus positive outer-finalizer release
and independent early-release, wrong-owner/lane, aliased-release and finally-before-sink negatives.

This decision does not authorize v4 meter implementation, B1 production work or destructive
execution. The repaired Design/interface/Work map must first receive a fresh different-actor QbD
`PASS` with zero blocker and zero advisory. Runtime targets, default `~/.omnimind` scope and all
protected exclusions remain unchanged.
