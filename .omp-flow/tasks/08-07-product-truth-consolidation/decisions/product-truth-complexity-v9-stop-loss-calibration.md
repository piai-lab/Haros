---
type: "Decision"
title: "Product-truth complexity v9 stop-loss calibration"
---

# Product-truth complexity v9 stop-loss calibration

## Selected route

The selected [stop-loss synthesis](../research/product-truth-complexity-v8-stop-loss.md) chooses
Route B: create a new, narrower Product-truth complexity v9 authority and keep every v8 r1-r17
candidate, fixture, handoff and Review immutable failed evidence. V9 does not repair, relabel or
extend v8 and cannot cite an r1-r17 green authored suite as candidate acceptance.
The later human [safe-degradation calibration](product-truth-complexity-v9-safe-degradation-calibration.md)
retains this route while superseding this Decision's earlier public-shape/static-verdict detail.

The selected route retains only facts that a candidate-independent repository measurement can own:
the exact Design/Work membership, official predecessor-evidence selection and exact evidence
blobs/tuple, selected-Work path lifecycle, outside-Work presence/mode/blob equality, dependency bytes,
exact declaration identity/B0 absence/export-private disposition/non-self-authorizing first
materialization and report determinism. Literal graph/SCC/count facts remain deterministic
observations until Design authors a complete exact allowed-delta table.

V9 has no authority to classify raw/global/alias expressions, callbacks, RHS/subtrees or per-use
owner semantics. The accepted v7 B0 inventory and all v8 counterexamples remain provenance only.
They are neither v9 candidate verdicts nor permission for raw effects.

## B1 proof owner

B1 owns the displaced safety claim. Each exact owner must expose typed intent and sanitized results
through an owner-private injected real/verifier port. The generated real verifier remains fixed at
10 owners, 146 operations, 87 states, 85 concrete race cases and 65 concrete kill cases. A fresh
different actor must, at the same B1 SHA:

- run the complete verifier without candidate filtering;
- apply every hidden-mutation family retained by immutable v8 r1-r17, including direct global,
  wrapper, alias, destructure/namespace, assignment/RHS, nested expression, selector, call-argument
  and callback forms, with the four exact r17 callback-global forms and adjacent positives;
- deterministically inventory every raw root, raw import, loader reference and production adapter
  composition, record count plus canonical digest and resolve every item to an owner-private port or
  an explicit failure; and
- inspect the capability declarations, public type/API surface, real-port composition, verifier
  composition and manifest bijection at that identical SHA.

If a raw bypass can preserve every v9 fact and has no reproducible verifier or same-SHA Review
failure condition, Route B is falsified and returns to Design. The response is not another v9 syntax
exception.

## Reuse and replacement rule

Work follows the repository's current reuse-first ladder: preserve the adopted Synara and existing
repository mechanism, identify the exact OmniMind gap, wire or locally repair it, and replace only
after naming the unique owner, a reproducible falsifier and why wiring/local repair cannot close the
gap. V8's r1-r17 record falsifies its expression-combination candidate authority; it does not
falsify unrelated Product, Synara, v7 mechanical or v8 evidence-binding mechanisms.

## Preserved boundary and next gate

The destructive allowlist, protected exclusions, five Product Work boundaries, Work order and B1
behavioral universe remain unchanged. V1-v8 bytes and evidence remain immutable. This calibration
does not authorize v9 implementation, B1, destructive execution or any Product change.

The next entry is a fresh different-actor QbD over the revised PRD, Design, both Decisions,
[v9 interface](../interfaces/product-truth-complexity-v9.md),
[v9 measurement Work](../work/product-truth-complexity-v9.md), all five Product Works and the
[authored Work map](../work/index.md). Only a recorded human calibration after that independent audit
may authorize v9 implementation, and that QbD must reach 0 blocker and 0 advisory. A separate zero-finding different-actor implementation Review must
then accept the immutable v9 meter before B1 can be assigned.
