---
type: "Decision"
title: "Product-truth complexity v9 stop-loss calibration"
---

# Product-truth complexity v9 stop-loss calibration

## Selected route and post-r2 calibration

The selected [stop-loss synthesis](../research/product-truth-complexity-v8-stop-loss.md) chooses
Route B: create a new, narrower Product-truth complexity v9 authority and keep every v8 r1-r17
candidate, fixture, handoff and Review immutable failed evidence. V9 does not repair, relabel or
extend v8 and cannot cite an r1-r17 green authored suite as candidate acceptance.
The later human [safe-degradation calibration](product-truth-complexity-v9-safe-degradation-calibration.md)
retains this route while superseding this Decision's earlier public-shape/static-verdict detail.

The immutable [implementation Review r2](../reviews/product-truth-complexity-v9-r2.md) at
`9ab90b18762612811d7cbab270b97c50251d06eb` proves one remaining P1 in the same finite
membership/changed-path family: the meter defaulted paths outside `apps/**`, `packages/**`,
`scripts/**` and root `package.json` to skip, so an adopted Package source or manifest, a patch,
root build input or new adopted asset could change while the report still asserted exact outside
equality and unchanged dependency closure. The maintainer accepts this as a Design authority gap,
not implementation risk and not a reason to restore semantic grammar.

The attempted r3 implementation was correctly aborted and rolled back before producing a candidate
or handoff. The repository returned to r2 Review HEAD `9ab90b18762612811d7cbab270b97c50251d06eb`;
the v9 meter/config/test/fixture tree is byte-identical to reviewed r2 candidate
`ec416f7fe6eeea908fb80bbd6c716bc01e9860bd`, and only the three pre-existing shared-tree user
document edits remain outside the Bundle. The aborted attempt is not evidence, acceptance or an
implementation predecessor.

This calibration supersedes the implementation authorization in the earlier
[v9 PASS approval](product-truth-complexity-v9-pass-approval.md). That approval remains provenance
for the formerly approved Design `f110fb66006768074ca192bb94024632d16c09dd`; it cannot authorize
another implementation until the repaired Design receives the fresh 0/0 QbD and subsequent human
PASS required below.

The selected route retains only facts that a candidate-independent repository measurement can own:
the exact Design/Work membership, official predecessor-evidence selection and exact evidence
blobs/tuple, selected-Work path lifecycle, outside-Work presence/mode/blob equality, dependency bytes,
exact declaration identity/B0 absence/export-private disposition/non-self-authorizing first
materialization and report determinism. Literal graph/SCC/count facts remain deterministic
observations until Design authors a complete exact allowed-delta table.

For every Product candidate comparison, the candidate-independent rule is now closed:

- derive the comparison base only from the Main/human-selected official predecessor evidence row;
- enumerate the complete net Git changed-path set with rename detection disabled, so a move is a
  deletion plus an addition;
- reject every changed path by default;
- replace that default only for exact `production` members of the one selected Work, derived from
  the five frozen `omp-flow-production-boundary-v1` blocks; `measurement` and `dependency` members
  are not Product-candidate mutability;
- apply only the Design-authored accepted-presence/mode and exact first-materialization
  dispositions in the revised [v9 Interface](../interfaces/product-truth-complexity-v9.md); and
- give meter, config, fixtures, tests, extensions, directory names, root predicates and output or
  evidence categories zero authority to add an exemption.

The accepted-tree dependency/adoption byte set is separately Design-owned. At approved Design
commit `f110fb66006768074ca192bb94024632d16c09dd`, the immutable root
`README.md#source-adoptions` block, nine exact manifest paths, `bun.lock`, its declared patch root,
adopted source roots and legal paths expand to 6,321 complete
`path + presence + mode + Git blob + SHA-256` records. Their raw-JCS-sorted array digest is
`6687319b0ea58643812cee677fad03b3152e8bfcb31486ddb368bc1b3cf2f599`.
The Interface owns the complete input list and exact expansion algorithm; config may pin those
Design values but cannot choose a path. Candidate comparison applies the selected-Work rule to any
overlapping record and requires exact accepted bytes for every other record.

V9 has no authority to classify raw/global/alias expressions, callbacks, RHS/subtrees or per-use
owner semantics. The accepted v7 B0 inventory and all v8 counterexamples remain provenance only.
They are neither v9 candidate verdicts nor permission for raw effects.

## B1 proof owner

B1 owns the displaced safety claim. Each exact owner must expose typed intent and sanitized results
through an owner-private injected real/verifier port. The generated real verifier remains fixed at
10 owners, 146 operations, 87 states, 85 concrete race cases and 65 concrete kill cases. A fresh
different actor must, at the same B1 SHA:

- run the complete verifier without candidate filtering;
- apply every hidden-mutation family retained by immutable v8 r1-r17, including direct global,
  wrapper, alias, destructure/namespace, assignment/RHS, nested expression, selector, call-argument
  and callback forms, with the four exact r17 callback-global forms and adjacent positives;
- deterministically inventory every raw root, raw import, loader reference and production adapter
  composition, record count plus canonical digest and resolve every item to an owner-private port or
  an explicit failure; and
- inspect the capability declarations, public type/API surface, real-port composition, verifier
  composition and manifest bijection at that identical SHA.

If a raw bypass can preserve every v9 fact and has no reproducible verifier or same-SHA Review
failure condition, Route B is falsified and returns to Design. The response is not another v9 syntax
exception.

## Reuse and replacement rule

Work follows the repository's current reuse-first ladder: preserve the adopted Synara and existing
repository mechanism, identify the exact OmniMind gap, wire or locally repair it, and replace only
after naming the unique owner, a reproducible falsifier and why wiring/local repair cannot close the
gap. V8's r1-r17 record falsifies its expression-combination candidate authority; it does not
falsify unrelated Product, Synara, v7 mechanical or v8 evidence-binding mechanisms.

## Preserved boundary and next gate

The destructive allowlist, protected exclusions, five Product Work boundaries, Work order and B1
behavioral universe remain unchanged. V1-v8 bytes and evidence remain immutable. This calibration
does not authorize v9 implementation, B1, destructive execution or any Product change.

The next entry is a fresh different-actor QbD at
`qbd/product-truth-complexity-v9-authority-repair-audit.md` over the revised PRD, Design, both Route B Decisions,
[v9 interface](../interfaces/product-truth-complexity-v9.md),
[v9 measurement Work](../work/product-truth-complexity-v9.md), all five Product Works and the
[authored Work map](../work/index.md). Only a recorded human calibration after that independent audit
may authorize v9 implementation, and that QbD must reach 0 blocker and 0 advisory. A separate
zero-finding different-actor implementation Review must then accept the immutable v9 meter before
B1 can be assigned.

This is the last allowed repair in the v9 membership/changed-path family. If that later
implementation Review finds another bypass in this family—including a skipped Git path, a
meter/config/fixture-selected path class or exemption, an unowned lifecycle transition, or an
incomplete dependency/adoption target expansion—Main must not dispatch another implementation
repair. The result returns to Design and the Product-truth sequence stops pending a new human
decision. This stop-loss does not weaken Route B semantic safe degradation or any B1 evidence
obligation.

## Decision provenance

- Human calibration source: maintainer dispatch after immutable Review r2.
- Review actor: `product_truth_complexity_v9_review_r2`.
- Review receipt: `e2df1132541e4e408f05c7b30e213232`.
- Architect actor: `product_truth_complexity_v9_authority_architect`.
- Architect receipt: `36cbe9631a3347958ad2f92b4d7bc03c`.
