---
type: "Decision"
title: "Product-truth complexity v4 repair calibration"
---

# Product-truth complexity v4 repair calibration

## Human calibration applied

The maintainer directed Main to continue the aggressive, fastest high-quality path and not pause
for ordinary implementation choices. Main therefore accepts the independent
[v3 meter Review](../reviews/product-truth-complexity-v3.md) as `FAIL` and selects the narrow repair
path. Rejected immutable v3 commit `ee980e5c304943f856df74f364f6464996652bef` remains evidence and
must not be amended or relabelled.

The replacement meter version must repair both P0 findings as conjunctive machine gates:

1. discover Product database consumers from resolved opener/constructor/receiver symbols and
   handle/caller dataflow before Product classification; neutral module, wrapper, variable and
   parameter names cannot hide a raw Product path;
2. prove in each exact runtime owner that the complete legacy refusal dominates every reachable
   current-generation read/create/write/open/lock/mutation sink. A syntactically correct probe that
   runs after current I/O must fail.

The required fixture matrix includes independent generic-wrapper/raw-path and late-current-I/O
negatives plus positive Web, Product and service owners whose refusal dominates all current sinks.
No text occurrence, Product-looking name or after-the-fact sink allowlist may authorize a consumer.

This decision changes measurement authority only. It does not add a destructive target, weaken a
protected exclusion, alter default-root authority or authorize Product production work. The new
Design/interface/Work map must receive a fresh different-actor QbD `PASS` with zero findings; the
replacement meter must then freeze in a new immutable commit and receive a different-actor Review
`PASS` before any repaired B1 receipt is issued.
