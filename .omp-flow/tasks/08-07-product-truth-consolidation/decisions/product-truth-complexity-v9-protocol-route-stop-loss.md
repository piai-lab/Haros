---
type: "Decision"
title: "Product-truth complexity v9 byte-protocol and Review-route stop-loss"
---

# Product-truth complexity v9 byte-protocol and Review-route stop-loss

## Binding design disposition

The dispatched Design objective treats both P1 findings in the immutable
[final-authority Review](../reviews/product-truth-complexity-v9-final-authority.md) as binding Design defects.
Candidate `558de08f897e2131c9159d118944272191f48359` remains rejected. Its recorded handoff commit
remains rejected evidence, and this Design operation does not edit the handoff path. Every prior
`FAIL` Review remains byte-immutable; no Review may be overwritten,
relabelled or selected as accepted predecessor evidence.

This disposition supersedes the implementation authorization in the historical
[final-authority PASS approval](product-truth-complexity-v9-final-authority-pass-approval.md). That
approval remains provenance for the Design it audited; it cannot authorize the rejected candidate,
a repair, the next bounded realization or B1.

This decision preserves [Route B](product-truth-complexity-v9-stop-loss-calibration.md) and its
[semantic safe degradation](product-truth-complexity-v9-safe-degradation-calibration.md). V9 still
owns only candidate-independent repository facts. It gains no raw/global/alias/callback/RHS,
per-use, lifecycle-behavior or source-safety grammar, and every accepted 69-production-row,
70-verification-row, 110-row union and 6,321 accepted-tree fact remains unchanged.

## Exact repair boundary

Only one later implementation may be considered, and only after the gate below. It is a single
bounded realization of:

1. the exact raw-byte `git diff --name-status -z --no-renames` protocol frozen in the revised
   [v9 Interface](../interfaces/product-truth-complexity-v9.md), including fixture-free real
   Git-object positives and negatives that reach the existing downstream lifecycle classifiers;
   and
2. the one new immutable implementation Review output
   `reviews/product-truth-complexity-v9-protocol-route.md`, consumed literally by the Interface's
   B1 predecessor row, the v9 Work handoff expectation, the B1 entry stop and the Work map.

This is not an r4 shape repair. It may remove the whole-buffer text/no-NUL misuse, implement the
frozen byte parser, add only bounded parser/real-Git verification within the existing v9 Work
boundary, and bind the exact new Review path. It may not add path categories, lifecycle exemptions,
semantic syntax families, another Review alias/fallback, Product code, dependencies, another Work
or a second evidence route.

## Gate and terminal disposition

No implementation is authorized by this Decision. A fresh different actor must first write
`qbd/product-truth-complexity-v9-protocol-route-audit.md` over the revised PRD, Design, this Decision,
both earlier Route B Decisions, v9 Interface, v9 Work, B1 Work and Work map. The audit must report
exactly 0 blocker and 0 advisory. A human must then record an explicit `PASS` that authorizes only
the single bounded protocol/route realization.

The later implementation needs a different-actor zero-finding `PASS` at exactly
`reviews/product-truth-complexity-v9-protocol-route.md`. Any material implementation or Review
failure ends v9: no r4/r5 repair, same-path overwrite, alternate Review alias, grammar extension or
B1 dispatch is allowed. Main returns to stop/alternate authority and obtains a new human decision.

## Provenance

- Failed Review actor: `product_truth_complexity_v9_final_authority_review`.
- Failed Review receipt: `458b3ed4dd774137bdfd38fe1e5ff554`.
- Binding source: dispatched Architect objective; this Concept is not a QbD audit or human PASS.
- Architect actor: `product_truth_complexity_v9_protocol_architect`.
- Architect receipt: `1e07cf4d3605423583af31055172c50b`.
- Architect predecessor receipt: `ecb72ae80a9e46f4ba39b858fa003c4f`.
- Architect predecessor output: `handoffs/product-truth-complexity-v9.md`.
