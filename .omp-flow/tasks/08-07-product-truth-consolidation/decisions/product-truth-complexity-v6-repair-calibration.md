---
type: "Decision"
title: "Product-truth complexity v6 repair calibration"
---

# Product-truth complexity v6 repair calibration

## Human calibration applied

Main accepts the independent [v5 implementation Review](../reviews/product-truth-complexity-v5.md)
as `FAIL` under the maintainer's standing instruction to continue the fastest high-quality repair.
The immutable v5 candidate remains rejected evidence; it must not be edited, relabelled or used as
the current gate.

The selected repair is one v6 measurement authority built around a shared resolved-symbol,
event-based bounded ICFG and SSA/points-to must-analysis. The repair must replace classifier-specific
token/regex proof, source-position fallback and static-callsite lock identity. It must prove helper
return provenance, invocation-scoped fresh scratch and copy identities, reachable validation events
that dominate return/open, ordered close/remove/absence on every completion, per-acquisition tokens
across real loop backedges, release-capable scheduling and exact typed-reset terminal disposition
after catch/finally override.

V6 must inherit the full v5 matrix plus all eight Review failures and their adjacent positive
controls. Unsupported proof-relevant calls, properties, scheduling, recursion, loops, completions or
state/context bounds fail closed; no new regex, helper name, fixture name, temp-directory or
direct-tool exemption may discharge the proof.

This decision authorizes only v6 PRD/Design/interface/Work-map repair followed by a fresh
different-actor QbD. It does not authorize meter implementation before a zero-blocker/zero-advisory
QbD, B1 production work, destructive execution, dependency changes, Campaign promotion or any read
or mutation of real `~/.omnimind`.
