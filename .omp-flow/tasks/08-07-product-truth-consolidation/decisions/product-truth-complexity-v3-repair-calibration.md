---
type: "Decision"
title: "Product-truth complexity v3 repair calibration"
---

# Product-truth complexity v3 repair calibration

## Human calibration applied

The maintainer's standing option-1 direction retains A14 and the complete responsibility split,
repairs critical blockers directly and does not weaken the exact destructive boundary. Applied to
the [v3 authority audit](../qbd/product-truth-complexity-v3-audit.md), it selects repair of the
current scope.

V3 freezes path membership and Design-time edge/sink snapshots as evidence. Later exact paths may
materialize and may add edges only when both endpoints are already in the frozen exact path set.
Every candidate-time Product database sink is dynamically discovered and classified by the frozen
resolved-symbol/dataflow rules; no sink list is learned from or added by the candidate. A new
importer/target outside the frozen set, an unclassified/competing sink or unknown provenance fails.
Fixtures must prove one valid future Store edge/sink and the corresponding outside-set and invalid
sink failures.

B1 and all later Works consume accepted v3 only. Rejected v2 remains immutable provenance, never a
current semantic or universe gate. `bun.lock` exclusion is expressed through v3 dependency
classification. The Store source boundary is the one exact future file already named by its Work;
any private SQL file requires a new machine-boundary decision.

This decision changes measurement authority only. It adds no runtime behavior, destructive target
or protected-risk acceptance. A fresh different-actor QbD `PASS` with zero findings is required
before any v3 implementation assignment.
