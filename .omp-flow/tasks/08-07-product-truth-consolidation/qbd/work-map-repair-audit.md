---
type: "QbD 2 Audit"
title: "Direct first-public Product truth — repaired work-map audit"
---

# Direct first-public Product truth — repaired QbD 2

## Audit identity

- Bundle: `.omp-flow/tasks/08-07-product-truth-consolidation`
- Role: `qbd`
- Entry Concept: [`work/index.md`](../work/index.md)
- Audit output: `qbd/work-map-repair-audit.md`
- Bounded objective: fresh scoped re-audit of the human-calibrated path repair: verify the B1
  canonical Product-database probe owner, the complete Native Host v2 health/probe owner set, the
  literal accepted-handoff sequence and the tightened Store composition/probe boundary, while
  carrying forward the approved design, prior closed findings and unchanged acceptance coverage.
- Actor ID: `product_truth_qbd2_a2`
- Dispatch receipt: `acb5462258954f898ede377bd91df11e`
- Predecessor receipt: `d5fecd38c5e44f2e89614982c65b5f16`
- Predecessor output/handoff: `.omp-flow/tasks/08-07-product-truth-consolidation/work`

## Verdict

**PASS**

- Risk: **medium — intentionally destructive and implementation-sensitive, with exact boundaries
  and fail-closed verification**
- Decision-critical blocking findings: **0**
- Advisory observations: **0**

The repaired five-Work map can realize the approved PRD and Design without unauthorized path
expansion or simultaneous ownership. Prior QbD 2 B1 is closed because the immutable B1 Work now
owns the exact OpenCode production probe only for canonical Product-database path resolution and
requires its focused proof before the B1 commit. Prior B2 is closed because the Native Host Work
names the actual health owner and both production probes, each under a narrow v2 binding/readiness
purpose. The map now states the complete accepted-handoff order literally, and the Store Work names
the currently evidenced composition/probe paths while stopping for map repair on any additional
need. No new evidence contradicts the approved QbD 1 decisions or previously closed findings.

## Decision context and evidence separation

### Confirmed evidence

1. The human calibration selected exact boundary repair without changing A1-A15, the destructive
   scope, protocol v2, B1 meaning, the complexity universe or the ban on compatibility/migration
   ([`qbd2-path-repair-calibration.md`](../decisions/qbd2-path-repair-calibration.md)).
2. B1 now explicitly owns
   `apps/service/src/opencode/liveJourneyProbe.ts` solely to replace the retired Product database
   filename/path construction with the canonical resolver/path. Its done conditions require zero
   old path residue and equality with normal Service composition; its verification requires the
   focused probe check before the clean immutable B1 commit
   ([`direct-first-public-b1.md`](../work/direct-first-public-b1.md), Allowed boundary, Done
   conditions and Verification).
3. The existing OpenCode production probe still contains the literal `product-state-v1.sqlite`.
   That is the known implementation input identified by the prior audit, not contradictory evidence
   against the repaired map: the exact file and exact permitted correction are now B1-owned.
4. The Native Host Work now names the actual normal-runtime health file,
   `apps/service/src/product/health/nativeHostHealthMonitor.ts`, and explicitly owns
   `apps/service/src/native-host/liveJourneyProbe.ts` plus
   `apps/service/src/native-host/packageCrashProbe.ts`. The allowed changes are confined to v2
   binding construction, bounded authenticated readiness/error semantics and required process
   evidence ([`native-host-package-root-binding.md`](../work/native-host-package-root-binding.md),
   Allowed boundary and Verification).
5. All three repaired Native Host production paths exist. The health owner constructs the normal
   Native Host client, and both probes construct/use production Native Host clients, so the repaired
   boundary covers the concrete v2 consumers that made prior B2 decision-critical. Protocol, Host,
   Desktop and Service paths needed for the closed v2 handshake remain within the same Work.
6. The index now states the complete sequence literally: accepted B1 → accepted Native Host →
   accepted execution leaf → accepted Product State Store → Coordinator/facade C. B1 and Native
   Host share `apps/desktop/src/main.ts`; Native Host and the leaf share
   `apps/service/src/native-host/executionBoundary.ts`; later Store and C edits occur only after the
   preceding different-actor-accepted handoffs. The map expressly infers no shared-tree overlap
   ([`work/index.md`](../work/index.md), Hard ordering and Review and next gate).
7. The leaf remains a state-free contract-only extraction; Store owns one connection and complete
   Product transactions; Coordinator owns volatile handles/effects; and the facade retains exactly
   36 Web/RPC operations. The boundaries preserve the approved responsibility graph rather than
   adding a competing authority ([`product-execution-leaf.md`](../work/product-execution-leaf.md),
   [`product-state-store.md`](../work/product-state-store.md), and
   [`product-execution-coordinator-facade.md`](../work/product-execution-coordinator-facade.md)).
8. The Store Work now enumerates the currently evidenced production composition/probe paths:
   `executionBoundary.ts`, both Native Host probes, the OpenCode probe and `serverLayers.ts`. It also
   enumerates the four exact focused/integration proof paths that may change and makes any newly
   required composition, probe or test path a Work-map stop. Repository inspection confirms those
   named paths exist and cover the current Product-control-plane construction sites outside the
   monolith.
9. A1-A15 remain completely mapped: A1-A9 and B1/A14 to the immutable B1 Work; A13 to Native Host;
   A10 to Store; A11-A12 to Coordinator/facade; integrated A14-A15 to C. The three accepted QbD 1
   advisories remain hard B1 evidence conditions: fingerprint-registry bijection, whole-profile
   write trace and mechanically zero extraction surface at B1
   ([`work/index.md`](../work/index.md), Acceptance coverage;
   [`qbd1-pass-approval.md`](../decisions/qbd1-pass-approval.md), Advisory calibration).
10. The repaired QbD 1 PASS remains applicable: protected co-resident facts fail closed before
    deletion, Native Host v2 authenticates the exact lane/root before reads, `inspect` is read-only,
    and B0/B1/C use one frozen universe. No linked change weakens those decisions
    ([`design-repair-audit.md`](design-repair-audit.md)).

### Assumptions used

- The authored `work/index.md` hard ordering governs assignment even where a later individual Work
  repeats only its immediately relevant local entry stop. The index calls the sequence literal and
  prohibits assignment/start before the applicable accepted handoff.
- The allowed path lists are authorization boundaries. Their explicit outside-path stop is
  enforceable by implementers and reviewers rather than an illustrative suggestion.
- This is a pre-implementation realizability audit. Existing old literals and v1 construction are
  expected inputs when their exact owners and replacement proofs are now declared; implementation
  handoffs must still prove their removal.

### Strongest counter-evidence

- The OpenCode probe is still stale in the current production tree. This previously invalidated
  B1, but no longer does: B1 now owns that exact file, limits the edit to canonical Product database
  resolution, scans it for zero residue and runs its focused proof before freezing B1.
- The current health and probe clients still use the pre-v2 construction shape. This previously
  made A13 unrealizable inside the Native Host boundary, but all three production clients are now
  exact allowed paths with the same v2-only purpose and real-process verification.
- Several Works successively touch the same files. That would be unsafe under concurrent or
  branch-based execution, but the repaired map prohibits it: every overlapping step consumes the
  accepted predecessor handoff and the literal sequence permits no simultaneous shared-tree
  ownership.
- Final C uses broader wording for existing journey probes, but it enters only after all four prior
  handoffs are accepted and may change them only for final composition/facade/diagnostics. It cannot
  retroactively alter B1 or the Native Host contract and therefore does not create concurrent
  ownership.

### Accepted risk

The carried accepted risk is the intentional, unrecoverable loss of positively classified
pre-baseline Product, Automation/service and exact legacy Web-draft bytes under canonical default
`~/.omnimind`. Protected facts and every excluded target remain outside that authorization and must
fail closed. Residual implementation risk includes path/reparse and WAL stability, profile/process
races, whole-profile incidental writes, Native Host transcript races/resource bounds, preservation
of compound Store transactions, shared-file sequencing and live Provider/process variance. The
map assigns a specific stop or proof to each; none currently makes the approved core path
unrealizable or unjudgeable.

## Prior finding closure

### Prior B1 — immutable B1 excluded the OpenCode Product-database consumer

**Closed.** The exact production path is now B1-owned for the minimum canonical resolver/path
change, with structural zero-residue and focused probe proof required before immutable B1. C no
longer needs to repair B1 retroactively.

### Prior B2 — Native Host Work omitted or misnamed required v2 clients

**Closed.** The nonexistent health path was replaced by the actual Product health owner, and both
production Native Host probes are explicitly owned for v2 binding/readiness and process evidence.
No optional v1 construction or removal of required probes is needed.

### Prior advisory — make the Host/extraction edge literal

**Closed.** The index gives the exact accepted B1 → accepted Native Host → accepted leaf → accepted
Store → C sequence and identifies each shared path that motivates serialization.

### Prior advisory — tighten Store composition/probe wording

**Closed.** The Store Work enumerates the current production and proof paths and stops for map
repair if implementation finds another required caller. The boundary neither grants directory-wide
authority nor creates competing simultaneous ownership.

## Acceptance coverage judgment

| PRD acceptance | QbD 2 judgment |
| --- | --- |
| A1-A9 | Covered by indivisible B1, including the repaired canonical OpenCode probe path and all three accepted QbD 1 evidence shapes. |
| A10 | Covered by the sequential Store Work with one connection, complete command transactions and exact composition/probe ownership. |
| A11-A12 | Covered by final Coordinator/facade C after accepted leaf and Store handoffs. |
| A13 | Covered by Native Host v2 with the actual health owner, both production probes and dev/packaged real-process proof. |
| A14 | Covered by frozen instrument, immutable unsplit B1 and conjunctive C gates in one universe. |
| A15 | Covered by focused preservation in each Work and final isolated packaged/live journeys, without repeating unchanged evidence absent a trigger. |

## Decision-critical findings

None.

## Advisory observations and residual risk

No additional advisory is required for the repaired scope. Implementation and review must enforce
the written stops: a new path need, unaccepted predecessor, changed B1 SHA/instrument, second
connection/writer, optional v1 client, failed destructive guard or failed real-process/live gate
rejects the affected handoff rather than expanding authority or being carried as ordinary risk.

Residual risk remains medium until the immutable B1, Native Host process matrix, Store transaction
matrix and frozen C evidence exist. This is implementation evidence intentionally assigned by the
map; its absence before implementation does not prevent judging the map's realizability.

## Exact next human decision

This model `PASS` authorizes no transition by itself. The maintainer must record one of these
directions:

1. **Accept PASS and authorize execution:** execute the exact repaired sequence—accepted B1,
   accepted Native Host, accepted execution leaf, accepted Store, then C—with each next overlapping
   Work gated by its different-actor-accepted handoff.
2. **Request bounded tightening before execution:** change only a named advisory or path boundary
   while preserving the approved PRD/design and return the changed map to the applicable human
   decision point.
3. **Defer or stop** this checkpoint.

There is no unresolved `FAIL` or decision-critical `NEEDS_EVIDENCE`. The accepted QbD 1 destructive
boundary, protocol v2 decision, immutable B1 meaning, frozen complexity universe and g50 closure
remain unchanged.
