---
type: "Work"
title: "Freeze the authoritative Product-truth v5 meter"
---

# Freeze the authoritative Product-truth v5 meter

## Objective

Replace rejected immutable v4 measurement authority with one narrow v5 meter. V5 preserves v4's
accepted frozen universe, capability roots, path-origin authority, semantic counters and complete
fixture matrix, while closing the failed Review's five counterexamples with a production+direct-tool
capability domain, per-acquisition same-predecessor lock must-analysis and reachability-based refusal
CFG/ICFG. It also adds the QbD-required exact direct-tool classifier-copy origin and independently
derives that future flow's retired source, private scratch, copy/open and all-completion cleanup
invariants. This Work implements measurement only.

## Useful inputs

- [V5 repair calibration](../decisions/product-truth-complexity-v5-repair-calibration.md)
- [V5 QbD repair calibration](../decisions/product-truth-complexity-v5-qbd-repair-calibration.md)
- [Failed immutable v4 Review](../reviews/product-truth-complexity-v4.md)
- [V5 repair authority](../interfaces/product-truth-complexity-v5.md)
- [Inherited v4 authority](../interfaces/product-truth-complexity-v4.md) and its accepted
  [final QbD](../qbd/product-truth-complexity-v4-final-audit.md)
- [PRD R11/A14](../prd.md), [Design measurement gates](../design.md) and the five byte-identical
  product Work boundaries linked from the [Work map](index.md)

## Entry stop

Do not assign this Work until a different-actor QbD accepts the v5 PRD/Design/interface/Work map
with zero blocker and zero advisory at one immutable Design SHA. No Product production, dependency,
direct-tool behavior or user-state path may change before the later v5 implementation Review passes.

## Allowed code and output boundary

The implementer may create only:

- `scripts/product-truth/measure-complexity-v5.mjs`;
- `scripts/product-truth/complexity-universe-v5.json`;
- `scripts/product-truth/measure-complexity-v5.test.ts` and bounded fixtures under
  `scripts/product-truth/fixtures/complexity-v5/**`;
- `handoffs/product-truth-complexity-v5.md`.

V1-v4 code, config, tests, fixtures, reports, handoffs and Reviews remain immutable rejected
evidence. The five Work fences, Product code, direct-rebuild behavior, dependency manifests/locks,
architecture owners, Campaign state and user state are outside this Work.

## Done conditions

- Re-extract and pin the inherited five Work blocks, capability and owner-lock blocks, frozen
  membership, dependency forms and complete derived inventory exactly as v4 required. Separately
  extract and pin the additive `omp-flow-direct-tool-classifier-copy-authority-v1` block from the
  accepted v5 interface. Resolve its exact future `sqlite-classifier.ts` entry, locals and helper
  declarations and independently derive retired-source, scratch-root, copy-path, SQLite-handle and
  all-completion cleanup flow; neither block nor config assertions prove those facts. Run
  candidate capability discovery over every materialized `production` **and** `direct-tool` member;
  exclude direct-tool only from ordinary-runtime owner-lock proof.
- Implement one bounded explicit CFG/ICFG used by both refusal and lock analysis. Each acquisition
  creates a unique abstract token; actual predecessor meet preserves held only for the same binding
  and same token. Alias release, branch phi, loop, call/return, catch/finally and resource-scope edges
  update state; detached/unresolved scheduling is release-capable unknown and fails closed.
- Interpret present/absent polarity and actual reachability. Model zero-iteration `for`/`for...of`/
  `while`, one-or-more `do`, exact constant branches, abrupt completion and catch/finally. The exact
  typed throw must cut every legacy-present successor; swallowed or unreachable throws do not count.
  Unsupported proof-relevant flow fails closed.
- Preserve all v4 metrics and inherited fixtures. Add the exact direct-tool SQLite/DELETE, different-
  handle branch phi, detached microtask release, empty-iterable `for...of` guard and false conditional
  rethrow negatives. Add one exact positive for an exact retired target copied under an invocation-
  owned fresh private scratch root, opened read-only/no-create and cleaned/absent on every completion;
  add independent negatives for copied/opened current Product/service stores, source-in-place open,
  unbound temp/caller/environment path, raw current Product alias and missing/bypassable cleanup.
  Preserve reversed-null, negated-exists and all H1-H10/matrix tests.
- Freeze one clean dedicated commit containing only allowed v5 code paths. Record full SHA, accepted
  Design SHA, all authority/inventory/universe digests including the additive classifier-copy block
  and independently derived flow digest, byte-deterministic B0, per-owner CFG/lock witnesses,
  focused results, typecheck and v1-v4 immutability in the handoff.

## Verification

- Run focused authority, membership, dependency, candidate capability, path/handle provenance,
  classifier-copy origin/control flow, presence-polarity, reachability and lock-token tests. Each
  negative must fail for its intended single cause and include a bounded witness. A broad temp/tool
  exemption, source-in-place/current/raw path or incomplete cleanup must never discharge a
  capability.
- Run v5 twice against immutable B0 and require byte-identical complete JSON.
- Compare all v1-v4 bytes for immutability; none can satisfy a current gate.
- Run scripts typecheck and `git diff --check`; prove no runtime, dependency, direct-tool behavior,
  Work fence or user-state path changed and no real `~/.omnimind` was read.

## Expected handoff

Write `handoffs/product-truth-complexity-v5.md`. A different actor reviews it in
`reviews/product-truth-complexity-v5.md`. Only a zero-finding `PASS` authorizes a new B1 receipt that
names that Review receipt and immutable v5 SHA/digests as predecessor.
