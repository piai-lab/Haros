---
type: "Work"
title: "Freeze the authoritative Product-truth v7 mechanical meter"
---

# Freeze the authoritative Product-truth v7 mechanical meter

## Objective

Replace rejected v6 measurement authority with one smaller v7 meter that proves only frozen
structure, dependency/import closure, raw-effect ingress ownership, complexity and stable counts.
It must delete the assurance claim—not recreate the implementation—of v5/v6's runtime semantic
interpreter. This Work changes measurement only.

## Useful inputs

- [V7 Occam repair calibration](../decisions/product-truth-complexity-v7-repair-calibration.md)
- [Failed immutable v5 Review](../reviews/product-truth-complexity-v5.md)
- [Failed immutable v6 Review](../reviews/product-truth-complexity-v6.md)
- [V7 mechanical authority](../interfaces/product-truth-complexity-v7.md)
- [PRD R11/A14](../prd.md), [Design measurement gates](../design.md), and the five byte-identical
  Product Work boundaries linked from the [Work map](index.md)

## Entry stop

Do not assign this Work until a fresh different-actor QbD accepts the v7
PRD/Design/interface/Work map with zero blocker and zero advisory at one immutable Design SHA. No
Product production, dependency, direct-tool behavior, test verifier or user-state path may change
inside this Work.

## Allowed code and output boundary

The implementer may create only:

- `scripts/product-truth/measure-complexity-v7.mjs`;
- `scripts/product-truth/complexity-universe-v7.json`;
- `scripts/product-truth/measure-complexity-v7.test.ts` and bounded structural fixtures under
  `scripts/product-truth/fixtures/complexity-v7/**`; and
- `handoffs/product-truth-complexity-v7.md`.

V1-v6 code, config, tests, fixtures, reports, handoffs and Reviews remain immutable rejected
evidence. The five Work fences, Product code/tests, direct-rebuild behavior, dependencies,
architecture owners, Campaign state and user state are outside this Work.

## Done conditions

- Re-extract and pin the five Work blocks, exact frozen membership, v7 effect-ingress authority,
  accepted dependency manifests/lock bytes and all physical/conceptual counter definitions from the
  accepted Design commit. Config assertions may only bind these extracted facts, never add roots,
  owners, paths, packages or dispositions.
- Resolve the static import/export graph with the compiler and exact package export/lock closure.
  Reject outside-set endpoints, computed/unresolved specifiers, undeclared/range dependencies,
  scripts-only dependency escape and byte drift. Record exact package/version/integrity/source-entry
  digests.
- Enumerate every raw-effect ingress and binding reference in every frozen production/direct-tool
  member for filesystem, SQLite, LevelDB, process, Web Storage and ambient loaders. Require exact
  class/owner containment, stable closed-unrelated owner/class/domain set, B1/C owner move exclusivity,
  non-export/non-escape shape and global forbidden-loader absence. Report path, resolved symbol,
  owner and count without claiming path provenance or lifetime behavior.
- Preserve deterministic B0/B1/C physical lines, import/SCC direction, facade/table/database/writer/
  state-machine/gateway/transaction/sentinel/compatibility/Package-lifecycle stable counts. B0 may
  observe candidate nonconformance; B1/C hard-fail structural/effect-ingress/count violations.
- Contain no general CFG/ICFG, SSA, points-to, resource/token, Promise/task, Effect, catch/finally,
  scheduler or semantic-overlay engine. The test suite includes source-shape assertions that these
  forbidden analyzer authorities, virtual expected verdict input and fixture-name success branches
  are absent.
- Freeze one clean dedicated commit containing only allowed v7 paths. Record full SHA, accepted
  Design SHA, Work/effect-ingress/dependency/universe digests, deterministic B0, exact counters,
  focused results, typecheck, v1-v6 immutability and five Work-fence digests in the handoff.

## Verification

- Focused structural fixtures cover each static/dynamic/CommonJS/builtin/global ingress form,
  namespace/destructure/alias/re-export, literal allowed loader, computed/unknown/forbidden loader,
  owner-local reference, outside-owner reference, exported raw binding/handle/release and the B1→C
  Product owner move. Each negative fails one mechanical gate; no fixture asserts cleanup, lock,
  scheduling or exception semantics.
- Dependency fixtures cover exact pins, lock/source/export-map drift, scripts-only classic-level
  containment, outside-set/unresolved imports and byte-bound closure.
- Run v7 twice against immutable B0 and require byte-identical complete JSON.
- Compare every v1-v6 instrument byte and all five Work machine blocks for immutability.
- Run scripts typecheck and `git diff --check`; prove no production, dependency, test verifier,
  destructive scope or user-state path changed and no real `~/.omnimind` was read.

## Expected handoff

Write `handoffs/product-truth-complexity-v7.md`. A different actor reviews it in
`reviews/product-truth-complexity-v7.md`. Only a zero-finding `PASS` authorizes B1 to consume the
immutable v7 bytes and implement the separately owned runtime capability/trace/fault/race/kill
proof. The v7 reviewer must reject any meter claim that those runtime behaviors were statically
proved.
