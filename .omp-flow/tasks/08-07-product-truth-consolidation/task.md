---
type: "Task"
title: "Consolidate Product truth before Remote"
---

# Consolidate Product truth before Remote

Task directory: `08-07-product-truth-consolidation`.

Deliver the bounded post-F-13 checkpoint required by `execution-brief.md`: discard only the
maintainer-authorized pre-baseline development state under the canonical default `~/.omnimind`,
establish the first public Product schema baseline directly, remove unshipped steady-state
compatibility, split the current Product control plane by stable responsibilities, and prove that
production and conceptual complexity decrease before any Remote work begins.

This task does not enter Remote, expand the Engine catalog, redesign Package authority, or promote
any Campaign claim by itself. Destructive authority is limited to exact validated legacy Product,
Automation/service and OmniMind Web-draft state plus Package state proven duplicate/obsolete inside
the default home. Credentials, current first-public state, the current canonical Package generation,
Pi-native state, attachments, external ResourceRefs, user workspaces, Git, global configuration and
every other path are excluded. Product Service remains the sole Package-root owner and Native Host
receives, validates and loads only the explicit current-lane root.
