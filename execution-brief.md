# Execution brief

## 当前目标

Composer内置Slash Command presentation owner cut 已闭合，当前为`packaged-candidate / not-released`；没有获授权的后续代码任务。实现提交`1f354eb6f0`已由`main@fe74033ef58e500ce48f785b2ebfb5bb9e308a37`合并并进入本机候选安装版：16个canonical built-in的双语title、description、icon与localized search由一个穷尽Web descriptor拥有，Provider-native同名命令不借用built-in身份。后续证据提交只记录状态，不属于该安装版shipped bytes；未创建Release、未修改update feed。

## 当前协调与下一动作

- Source gates通过：focused Composer presentation合同104/104、菜单Browser 6/6、Web完整unit 327-file/4198-test suite、root 7-package typecheck、root lint与production build；lint为0 error，既有warning未扩张，React Compiler hot-path未新增bailout。
- 从已推送的`fe74033ef58e…`独立干净clone构建arm64 DMG；DMG SHA-256为`43488841cf6f49bebfc65285c410756e3152537651502b8382dbc60fedc6159a`，安装版`app.asar` SHA-256为`5794ac95372ac6cdb553db834bb87e93d5d1e42f710a09d50d5d9e2e8a79b6ac`。
- 任务专用HOME、userData、OmniMind home、Pi Agent目录和XDG配置下，Main、Renderer、Helper与bundled Server隔离成立；安装版通过简中/英文、light/dark、900×700、localized query、Enter/Escape/focus return与App关闭重开。未调用真实Provider或模型。
- 完整Browser并发套件中的Timeline中文时长、footer geometry与Terminal暖色期望仍是未触达main可复现的基线失败，未写成本任务通过。任务分支本地/远端引用已删除，隔离进程已停止，DMG已卸载；下一步仅在新授权到来时切换本brief的当前目标。

## Stop-loss

- 不把上述portfolio合成全仓重写，不因文件大或行数多机械拆分成熟生命周期owner。
- 不新增Settings/Command/Provider god registry、通用form/JSON DSL、第二store/writer/cache、watcher、daemon、Session Registry、runtime i18n平台或证据ledger。
- 同一事实若仍要求多个consumer手写清单、顺序、palette、schema、capability或fallback，必须继续`SIMPLIFY`；不能用`keep in sync`注释、同步清单或只加parity test供养明显可删除的第二真相。
- 当前工作、阻塞和下一动作只在本文件维护；稳定合同进`architecture/`，固定证据进`research/`，production adoption进根`README.md`，Campaign claim状态进`missions/independent-omnimind-v1.md`。关闭关注点时本brief应缩短或切换pointer，不再追加永久历史。
