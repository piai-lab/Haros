# Execution brief

## 当前目标

全局主题系统当前处于`global-theme-system-candidate / not-released`。本轮没有把Browser与Curator当作两个页面补丁，而是收回到单一Appearance owner：主题预设ID/顺序/明暗seed由同一catalog投影，普通DOM表面只消费semantic tokens，Terminal/Diff消费各自的窄语义投影，Browser annotation与Curator/observer等隔离surface只接收Contracts唯一拥有、运行时完整校验的credential-blind resolved snapshot；bundled OmniMind缺失或收到畸形snapshot时fail closed，upstream/default profile才可使用有界fallback。snapshot由App根appearance composition发布，不再依赖ChatView挂载；B类surface继续冻结创建时快照，新建或exact reopen才消费新主题，不新增live-sync控制面。主题preset保持作者既有的可编辑模板语义，已持久化palette不会因同ID seed更新被静默覆盖。第三方网页、PDF/图片/视频、Device Screen、导出内容与Provider/品牌资产保持原色/原内容。

source门为root test 10/10：作者与OmniMind Web Access package 561/561、Web 4190/4190、Server 4405/4405、Contracts 264/264、Shared 575通过/1跳过；root typecheck 7/7、lint 0 error（601个既有warning）与production build 5/5已通过。第一次root全量的唯一失败是untouched `pi-web-access` GitHub SIGTERM-resistant process-tree竞态，精确用例与完整package复跑通过，第二次root全量绿色；该失败没有被写成主题回归或用测试改写掩盖。

从exact pushed implementation SHA `9c4740da87a894913f30f4b3648340a8d2ef3cdc`的独立clean clone构建并安装arm64 DMG；DMG SHA-256为`8da30689b1393df6ec462ae19b3656739b17851b8a18d7a039b316fbf08d1451`，DMG内与安装版`app.asar` SHA-256均为`c30750bd7727480865ca13aa2fee583def5e6aef09d1b444779b55cbfb05d220`。任务专用HOME、OMNIMIND_HOME、PI_CODING_AGENT_DIR、XDG_CONFIG_HOME与userData下的Main、Renderer、Helper、bundled Server参数及Server cwd/state文件均复证隔离，未读取真实profile。明显暖色light/dark自定义palette已在真实安装版贯穿普通Shell/Composer、Appearance Settings、Browser chrome/local state、annotation overlay与Terminal；第三方青/珊瑚/黄网页保持原色，Terminal在主题切换和renderer reload前后保持同一PTY PID，简中/英文、1100×760窄窗、地址栏键盘focus、App关闭重开和主题持久化通过。正式Curator Server + 实际页面另以同一精确源码验证390×740、reduced-motion下的中文暖色observer与英文暖暗review，body/文本消费完整snapshot、结果可键盘折叠、无水平溢出、Provider资产无filter；该证据不冒充token进入普通Browser或完整Web Access packaged journey。Diff/code的暖色语义由同一SHA的`ThemeSurfaceInheritance`、`GitPanel.diffIdentity`及production build保护；安装版只到达暖色Diff框架而未取得真实彩色hunk，因此不扩写为packaged Diff hunk证据。当前状态足以称全局主题系统candidate，但仍不是签名、公证、Release、update feed或整个产品已发行。

六组内置工具的 Agent/Chat/Studio 策略已由exact pushed code SHA `586c9661e5`实现并通过merge `d4989f5cee`进入main。该code SHA已重建Desktop/DMG、替换安装，并用任务专用`userData`、home与Provider private home完成隔离fresh-profile验收；post-merge focused gates与root typecheck均通过。当前没有未完成的Host矩阵产品施工；本交付仍是本机ad-hoc candidate，不是签名、公证或Release证据。

`pi-web-access@0.24.1` → `@omnimind/om-web-access`已达到`production-adopted-with-packaged-web-access-evidence`：exact upstream baseline、P1–P6、bundled Agent composition、唯一config owner、Pi active-set、typed Curator/Browser/Timeline lifecycle、Settings与credential-blind Provider projection均已进入任务分支并通过合并终审。维护者要求的26个runtime identity全部使用身份准确、保持原色的本地品牌资产已由exact pushed implementation SHA `4df9de2474021c1b9396931307acbdb91ee16094`实现并通过隔离Settings/asset gate；默认`auto-summary`与agent-visible responseId continuation分别由exact pushed implementation SHA `52c8a25e75f702baef36b93fb1f8cc42f270897e`和`3f4d673bce30465cba387df2667d2488a744c05f`取得真实DeepSeek/MiMo局部packaged证据。workflow与Right Dock展示解耦、默认关闭的搜索过程展示、`auto-summary/none`非阻塞observer、`summary-review` pending审查、OmniMind profile专属默认与作者file-level工具开关已由exact pushed implementation SHA `ff67a21a97f9071bd9162dfb61e9f4b632a903a8`实现；同一安装候选现已完成四种workflow×展示主路径、keyed Tavily draft/named route、responseId继续取回、A/B多Thread background attention、exact reopen、call隔离及关闭重开无假入口的任务隔离packaged验证。该状态只证明Web Access adopted integration的安装产品链，不等于签名、公证、Release、update feed或整个OmniMind `packaged-product-proven`。它仍只属于bundled OmniMind Agent，不是AgentGateway/第七组Host/stock Pi能力。

exact pushed implementation SHA `9d9930f21b654315af9d164ac96b8fcdd59aecef`在前一truthful Provider/Settings候选上补齐route-aware主状态、删除Gemini第二结构判断入口、为Gemini账号诊断提供恢复、修正summary-review生命周期文案，并把Curator结果展开改成observer/review均可键盘操作的真实控件；summary inspector打开时背景settlement区从视觉、Tab顺序与accessibility tree中移除，review Provider切换前明确预告“持久默认+重搜+可能消耗额度”。route/Curator/Settings focused反例、Settings Browser 6/6、Curator真实page/server 4/4、root typecheck 7/7与Web production build已通过；完整package第二次运行557/558，唯一失败仍是untouched baseline GitHub SIGTERM-resistant process-tree竞态，首次并发运行中偶发超时的Gemini command用例已单独复跑通过。从该精确pushed SHA的clean clone构建并安装的arm64 DMG SHA-256为`2bbe0b24c140b709340e70bb13dd174fc1b0c664fa048fe7f6eee9f3c1573610`，DMG内与安装版`app.asar` SHA-256均为`ab7d83a1bb9a5beb38acfc882d04a94b488ce790f91d4b02aa0f02b3d76121f6`。fresh任务profile证明named Tavily缺少凭据时首屏准确显示“请设置当前选择的服务”，不再与Provider详情矛盾；复用的任务专用测试profile证明review页面为中文本地化标题、显示Provider切换的持久默认/重搜/额度后果，结果展开投影真实button与`aria-expanded/controls`，summary inspector打开时背景action bar同时`hidden`、`inert`、`aria-hidden`、不可见且零可聚焦控件。正式page/server Browser测试4/4另行证明Enter/Space键盘展开；本次短生命周期packaged DOM观察不冒充该键盘事件的安装全链证据。terminal后无waiting/reopen，App关闭重开后只有主页面、零loopback surface与零假Curator入口。该状态仍只是Web Access安装候选证据，不是签名、公证、Release、update feed或整个OmniMind产品发行。

exact pushed implementation SHA `6a0990cc7c3d22eb531a4d428972fbbd65a3444f`进一步闭合summary inspector焦点生命周期：首次进入已就绪的Inspector时焦点进入可理解的summary标题，返回时恢复到原触发控件或可见结果阶段fallback，不在重生成时反复抢焦点，也不新增focus trap或平行状态机。真实page/server E2E 4/4、focused Curator 22/22、完整作者套件558/558、root typecheck 7/7与Web production build均通过。从该精确pushed SHA的clean clone构建并安装的arm64 DMG SHA-256为`61da05da40b2bc86b31367cc58242802dac4e4327f8241c0ef443c4d8cc92f2e`，DMG内与安装版`app.asar` SHA-256均为`8d3131e704b94e48c6aaa71f908747bfa520601ae8ad7a8d01b69f59b69a64e1`。任务隔离packaged review证明打开后active element是Inspector内可见的`summary-heading`，返回后是可见结果阶段的`btn-send`，两者都不是`body`或隐藏元素；raw settlement后无waiting/reopen，App关闭重开后CDP只有主页面、零loopback与零waiting，任务配置恢复且保持`0600`，所有候选进程已关闭。该状态仍只是Web Access安装候选证据，不是签名、公证、Release、update feed或整个OmniMind产品发行。

合并终审后的exact pushed implementation SHA `d7017961b13c9604799c833e340bfc27fc9245c7`补齐四个真实边界：OmniMind presenter rejection与Curator handoff fail closed并typed-error settlement，不回退泄漏token URL；named Provider错误投影移除不存在的Pi/TUI恢复指令；OmniMind profile强制Curator loopback-only；Settings draft probe的single-flight按scope、Provider与request identity隔离。测试基础设施同时锁定受支持Node、作者套件并发上限、临时npm root隔离、Web E2E runner边界、完整默认config bytes与本地化Web Surface title；legal metadata已闭合到241个组件。最终source门为完整root test 10/10（package作者套件560/560、Web 4183/4183、Server 4404/4404）、root typecheck 7/7、lint 0 error（602既有warning）、production build 5/5、legal check通过。该SHA从远端clean clone生成arm64 DMG SHA-256 `844066e944057e169d9a80f12a00d8169fcce62275843759f65e32c14fce8ec6`，DMG内与安装版`app.asar` SHA-256均为`876e9cfe2fe2c34f9f682fb3c73a11c87b65b4d90f1e14cc4d4ddf85ba1e04be`。fresh任务profile复证Main、Renderer、Helper与bundled Server隔离；首次进入网络搜索生成`0600`配置且bytes显式为`provider:auto`、`workflow:auto-summary`、`autoOpenBrowser:false`；中文Settings主路径正确，关闭重开后无loopback token、waiting review、Right Dock假Tab或过期恢复入口。此前exact-SHA keyed Tavily、MiMo/DeepSeek、observer/review与A/B多Thread证据继续保护本轮未改动路径。当前任务候选达到production adoption合并门，但仍不是签名、公证、Release、update feed或整个OmniMind产品发行。

任务tip `7931d5be5286d5b067392c65438cadf769790003`已通过no-ff merge `6875e6275d0d4b132a342e5507e79b0065d3ced7`进入并推送`origin/main`。从该精确pushed main merge SHA的clean clone构建的arm64 DMG SHA-256为`11d309092a3e0441c99cfff1347db44419886ecb362593b1bd6894a6deae535f`，DMG内与当前安装版`app.asar` SHA-256均为`2f5d9bf686d1f48fb1269a478a7116079f9a4ceeef574c9877beef6c26168825`。fresh main任务profile再次复证Main、Renderer、Helper、bundled Server隔离，首次Settings创建的`0600`默认bytes、中文Agent-native主路径及关闭重开后的零loopback token、零waiting review、零假Tab均成立。此段是post-merge evidence；记录它的尾随提交不属于上述已测试Desktop bytes，也不改变not-released状态。

## 唯一 owner 与当前事实

- Contracts只拥有group ID、Settings/DTO值合同和窄`agent|chat|studio` transport vocabulary；Shared pure policy唯一拥有六组×三面的support/default矩阵，ProductSurface仍从authoritative `Project.kind`当次推导。
- ServerSettings唯一拥有用户intent、v1/v2/v3→v4迁移、bounded unknown、quarantine、整字段override replacement、revision与settings原子快照；v4单surface上限40可同时保留旧32个unknown与known intent，不存在第二settings store、migration marker或永久legacy双读。
- AgentGateway唯一拥有canonical catalog、runtime availability、Desired/Delivered projection与每次`tools/call`实时authority；Web只渲染一次原子snapshot生成的完整read model并提交完整next override map，不拥有policy。
- Web对mutation响应丢失只按回读canonical override map裁决：一致即accepted，不一致才回滚，回读失败保持“无法确认”且不自动重发；非pending cell直接消费Server的`configuredEnabled/effective`。
- Chat默认只有Browser；Goals与Automations支持但默认关，可由用户明确开启；Tasks与Diagnostics不支持，Device支持但默认关。Studio按独立Studio ProductSurface解析，而不是误用其`chat` ProviderWorkSurface。
- Host guidance与Session schema同生命周期。关闭立即拒绝旧Session的新call；重新开启只在新Session或真实native reload后提供。Todo、Engine-native tools、Skills、Prompts、Packages、Extensions、sandbox/approval和Browser/Device人类UI均不受此矩阵控制。
- Goal continuation与Automation新run在既有lifecycle admission重新消费当前surface policy；关闭Chat Goals使用既有pause，关闭Chat Automations使未admit新run进入既有failed路径，不新增状态机且不伪取消in-flight。
- Web Access的runtime Provider定义、canonical `.omnimind/agent/web-search.json`与credential-blind presentation projection是三个通过stable ID汇合但互不接管的owner；Web/Curator不得复制26家清单，品牌资产命中不得决定routing/availability。搜索全局不可用只收缩该Extension自己的`web_search/source_check`，不整包移除两个content tools。
- fork package-owned config service必须同时服务Settings与Extension，Settings不能为读写配置启动Session；App/Session启动不发Provider health probe。Provider测试与重新检查都是用户显式触发的最小真实请求，可能消耗额度。
- Provider测试允许使用当前完整未保存draft作为request-scoped candidate snapshot；不保存、不改routing/active-set或永久状态，并按显式request identity single-flight。单call terminal、Run abort、session tree与session shutdown必须按各自scope清理，fatal Curator/Host失败typed-error settle，不能把全实例cleanup挂在任意terminal上。
- config service唯一拥有schema与known migration，保存/刷新后只发process-local revision invalidation；live Extension实例各自恢复自己移除的工具，不建全局Session registry、file watcher或第二持久状态。测试/重新检查pending期间single-flight，避免重复计费。
- untouched baseline的三个P4反例是`browser_open reuse:true`导航当前Tab、Timeline假重开与token URL进入recent-history；当前source candidate已以Browser owner内部typed dedicated-tab seam、per-call ephemeral handle、exact pending reopen与non-history projection闭合，focused tests证明三个反例不再出现在候选路径。exact `ff67a21a…`安装候选的A/B双Thread验证进一步证明后台review不抢route/Right Dock、关闭A observer不settle B、B pending activity能精确重开、批准只settle B，且terminal/App重开均不留下假reopen或过期surface。
- Curator ephemeral Tab已裁决为internal-only：隐藏`Open externally`、raw-link copy与raw token地址；来源链接打开普通OmniMind Browser Tab，之后遵循一般Browser合同。只在owning Thread前台时自动呈现；后台Thread用既有waiting activity/attention，不抢route或Right Dock。
- Web Access新默认是`auto-summary`，普通联网在后台摘要后同turn继续；`summary-review`只由Settings显式选择、per-call override或用户明确要求审查来源时进入。已有显式配置不被默认变化覆盖，也不新增迁移平台。

## 已闭合证据与剩余顺序

1. focused/full source gates、root typecheck/build、changed-path lint与Settings Browser 10/10已通过；全量并行Browser harness只保留已证实与本diff无关的baseline/负载边界，不用旁支Timeline改动刷绿。
2. exact pushed `586c9661e5` 生成DMG SHA-256 `cd97b9dd…51ece`，DMG与安装后的`app.asar`均为`3cdb313e…b784`；安装版进程使用隔离profile且bundled Server来自安装包。
3. 安装版中文/英文矩阵、Agent/Chat/Studio默认、unsupported文案、真实815px窄屏分层、Chat Goals写入`revision=1`、关闭重开恢复均通过；App最小窗口约815px，390px由浏览器组件回归覆盖。
4. merge `d4989f5cee`后的Shared/Contracts 15项、Server 412项、Settings Browser 10项及root typecheck 6/6通过；另以独立测试维护提交修正两个既有`@synara/contracts`陈旧导入。真实用户profile与并行research修改未被读取、改写或清理。
5. Web Access默认`auto-summary` implementation candidate `52c8a25e75f702baef36b93fb1f8cc42f270897e`的package runner为545/546通过，新增行为测试证明默认调用完成后台摘要且不请求Curator；唯一失败仍是untouched baseline已复现的GitHub SIGTERM进程树竞态。root typecheck 7/7、Web Settings Browser 2/2、focused Server composition/config/default bytes/Settings projection均通过。该source gate不替代从pushed SHA取得的packaged证据。
6. 历史pushed SHA `286df13768`生成DMG SHA-256 `228796d58d64d3f2e3544e0a340fc6115db75b87fa8786e5152fa64de458b0c9`，安装版与构建候选`app.asar`均为`4f4abbefef2b7011da70bdac57eccb48b3071053798783f0ab66967a23bbfadb`。该fresh隔离实例曾证明13个品牌asset与13个中性mark可渲染、canonical默认配置为`0600`，但已被26家全部原色品牌asset的新标准取代，不能继续作为当前视觉交付证据。
7. exact pushed SHA `4df9de2474021c1b9396931307acbdb91ee16094`生成arm64 DMG SHA-256 `a02aff4c4ce3945fbbd9800a18f3a639bf5419c73fdc2a53b4dfe382f74e264e`，DMG与安装版`app.asar`均为`2e40168b9a644c609de94204bab297dec776bd0072852009f14ba3cc3b067b56`。隔离进程证据确认Main、Renderer与bundled Server全部使用`/tmp/omnimind-web-icons-journey.35BS86`下的任务目录；packaged Settings在light/dark与关闭重开后均为26个loaded local assets、25个unique paths、0 neutral、0 CSS inversion。该证据只关闭Settings/Provider-asset安装门，不替代真实搜索、Curator或发行证据。
8. exact pushed implementation SHA `52c8a25e75f702baef36b93fb1f8cc42f270897e`生成arm64 DMG SHA-256 `118b55370cbec44308ec68ecbeb5a0efd3bd0d50b88665284e3274aca09c2931`，安装版`app.asar` SHA-256为`018d004a888344e395fa9885f3be2493a1efd9ec0cc15cbaa0c32d4dc26eaeb4`。fresh任务profile核验Main、Renderer与bundled Server全部使用同一隔离目录；canonical配置以`0600`写入显式`provider: auto`与`workflow: auto-summary`。真实DeepSeek Agent通过keyless Exa完成默认search → background summary → same-turn continuation且未创建Curator；显式`summary-review`创建dedicated ephemeral Tab、保持pending、批准后terminal清理并让同一turn继续，关闭重开后Settings仍投影推荐的`auto-summary`且没有假Curator入口。DeepSeek最终措辞没有完全遵循“一句话总结”，因此该证据只证明工具/Curator生命周期和continuation，不证明模型答案质量。该SHA当时尚未闭合的MiMo stored-result衔接已由下一条exact candidate解决；keyed搜索Provider与多Thread packaged覆盖仍待完成。
9. exact pushed implementation SHA `3f4d673bce30465cba387df2667d2488a744c05f`生成arm64 DMG SHA-256 `4160ec9594e0cbc185b970a645be0e05695344682c255aa9f5bcaed02f831e18`，DMG内与安装后`app.asar` SHA-256均为`3d909eba51ea301e66f8ca71f522fbc1eccf7c98905ca4c46c906e06f767be4`。Main、Renderer与bundled Server进程参数复证三者均使用同一任务隔离profile，canonical文件保持`0600`及显式`provider: auto` / `workflow: auto-summary`。MiMo-V2.5-Pro真实调用产生非错误`web_search`，tool result同时含agent-visible Artifact responseId、`details.searchId`与`details.responseId`，随后真实`get_search_content`以该ID成功取回stored result；默认路径未创建Curator。同一精确安装候选下，DeepSeek V4 Flash显式`summary-review`又一次完成dedicated ephemeral Tab、批准、terminal cleanup与same-turn continuation。focused lifecycle 4/4、root typecheck 7/7和作者套件545/546通过，唯一失败仍是untouched baseline GitHub SIGTERM进程树竞态。授权Tavily draft-test按正式request-scoped runtime发出最小请求且canonical bytes未改变，但外部资源返回HTTP 433，因此不能写成keyed Provider成功。
10. source implementation SHA `2bf044049d7eeb419975d7426e5d6414d2136814`实现独立presentation设置、observer/review分层、file-level工具开关、profile隔离默认、V7内容流与Provider原色资产投影。作者完整套件545/546通过，唯一失败为同一untouched GitHub SIGTERM进程树竞态；P1/observer 9/9、typed Browser/Server 58/58、root typecheck 7/7、Web production build与Server ESM启动/本地`marked`解析通过。从该SHA构建的DMG在隔离真实DeepSeek + keyless Exa observer journey中证实搜索、后台摘要、same-run continuation与独立Tab均发生，但页面仍错误调用review `/summarize`并显示只读错误，因此该SHA和该DMG都不能作为observer通过证据。
11. source implementation SHA `fb66ac263ac6ccc18878e898dcdec897a6665b3a`先把observer页面workflow固定为non-review并移除重复terminal状态；clean-clone declaration build修复与observer chrome/accessibility收口随后进入exact pushed implementation SHA `ff67a21a97f9071bd9162dfb61e9f4b632a903a8`。该SHA的作者完整套件549/549、focused Curator 4/4、Browser逻辑25/25与root typecheck 7/7通过。
12. 从exact pushed implementation SHA `ff67a21a97f9071bd9162dfb61e9f4b632a903a8`的clean clone构建arm64 DMG，DMG SHA-256为`16386d1d8e01c2bd0ca5b2903485bfb5f7651c0fb867df348df458e00084cf1e`，DMG内与安装后`app.asar` SHA-256均为`59e6183a0a2e19e4aaa3886249e049fe77dc7f78d4f034ef8b6ccbc892b25844`。fresh任务profile复证Main、Renderer与bundled Server全部使用隔离目录，默认文件为`0600`并显式写入`provider: auto`、`workflow: auto-summary`、`autoOpenBrowser: false`。真实DeepSeek + keyless Exa通过：`auto-summary`展示关不打开Right Dock且同Run继续、展示开打开non-review observer且不pending；`none`两种展示状态均直接继续；显式`summary-review`保持pending并在批准后继续；in-flight关闭observer不取消搜索；agent-visible responseId可继续调用`get_search_content`；关闭重开后只有主页面且无waiting、假reopen或过期observer。packaged截图为`/tmp/omnimind-web-observer-final2-packaged.png`，SHA-256为`da115782356b25dcae6dfc55eef43ae45f827b4a9e98b0e8aed1acfd328db10a`。该证据关闭最终observer/presentation安装门，但不替代仍缺失的keyed搜索成功与多Thread packaged门。
13. 同一exact implementation SHA与安装`app.asar`下，replacement Tavily凭据先经Settings“测试当前未保存草稿”走正式runtime成功；pending期间按钮禁用且重复点击未发起第二request identity，canonical bytes、mtime、`provider: auto`、`workflow: none`、`autoOpenBrowser: true`及当前Session搜索工具均未改变。随后只在任务profile显式保存并执行named Tavily搜索，真实结果带Tavily attribution与agent-visible responseId，同一Run以`get_search_content`成功继续取回；测试后canonical文件经同一Settings owner恢复到原始SHA-256 `4f615180…dc5d`、无Tavily字段且仍为`0600`，剪贴板已清空。A/B双Thread安装journey中，A以前台`auto-summary`多query observer运行，B在有界延迟后于后台进入`summary-review` waiting；B没有自动导航A或打开review，关闭A observer后B仍pending，从B Timeline exact activity重开并批准后只settle B，A/B分别继续到各自terminal。terminal立即移除两边reopen；App关闭重开后CDP只存在主页面，A/B均无waiting/reopen或ephemeral Web Surface，bundled Server仍只打开任务profile的state DB。该证据关闭此前剩余的keyed Provider与multiThread Web Access安装门，但仍不宣称Release、签名、公证、update feed或整个产品发行完成。
14. source candidate `7335df428655fcb67919bc2e24fa52651abaed8a`收口Provider/Curator用户真相、descriptor同源projection、Settings IA与双语恢复。OpenAI/xAI Session路径、Gemini替代credential groups、Bright Data部分配置、当前但未配完整的named Provider、late/cancelled probe、file-level工具关闭、Provider持久化与multi-query success/partial/failure/no-query均有focused反例；作者与OmniMind package套件557/557、真实Curator page/server 2/2、Settings Browser 5/5、root typecheck 7/7和Web production build通过。首次packaged视觉探针准确发现routing/workflow触发器显示raw enum，该SHA已在同一Settings owner内改为本地化用户值并以浏览器断言锁定；当时尚需从后续精确pushed SHA重新构建Desktop并复验受影响的Settings、localized Browser title、observer/review及Provider switch truth，该门现由第15项闭合。
15. 从exact pushed implementation SHA `9d9930f21b654315af9d164ac96b8fcdd59aecef`的clean clone构建arm64 DMG，DMG SHA-256为`2bbe0b24c140b709340e70bb13dd174fc1b0c664fa048fe7f6eee9f3c1573610`，DMG内与安装后`app.asar` SHA-256均为`ab7d83a1bb9a5beb38acfc882d04a94b488ce790f91d4b02aa0f02b3d76121f6`。fresh隔离Settings-first journey验证默认文件`0600`及显式`provider: auto`、`workflow: auto-summary`、`autoOpenBrowser: false`，并经真实UI保存named Tavily缺凭据状态，首屏与Provider详情共同准确显示需要设置。复用的任务专用测试profile只用于受影响review表面：Main、Renderer与bundled Server参数均指向任务目录；真实review页面投影中文标题、Provider切换副作用预告、可聚焦结果button与`aria-expanded/controls`，summary inspector打开时背景action bar从视觉、Tab顺序和accessibility tree移除；terminal后Timeline没有reopen，关闭重开后CDP只有主页面且无loopback或waiting。任务配置经同一Settings owner恢复到`provider: auto`、`workflow: none`、`autoOpenBrowser: true`并保持`0600`，所有候选进程均已关闭。真实键盘激活由同一正式page/server Browser回归4/4覆盖，不能把本次packaged DOM观察扩写成安装环境的键盘事件全链；既有`ff67a21a…`真实workflow、Tavily与A/B多Thread安装证据仍按未改动生命周期继续成立。
16. 从exact pushed implementation SHA `6a0990cc7c3d22eb531a4d428972fbbd65a3444f`的clean clone构建arm64 DMG，DMG SHA-256为`61da05da40b2bc86b31367cc58242802dac4e4327f8241c0ef443c4d8cc92f2e`，DMG内与安装后`app.asar` SHA-256均为`8d3131e704b94e48c6aaa71f908747bfa520601ae8ad7a8d01b69f59b69a64e1`。Main、Renderer与bundled Server再次复证只使用任务专用profile；真实packaged review中，summary inspector ready后焦点位于Inspector内可见的`summary-heading`，点击返回后恢复到可见结果阶段的`btn-send`，均不是`body`或隐藏元素。raw settlement后无waiting/reopen；App关闭重开后只有主页面、零loopback surface与零waiting，任务配置已恢复且保持`0600`，所有候选进程均已关闭。真实page/server E2E 4/4、focused Curator 22/22、完整作者套件558/558、root typecheck 7/7与Web production build通过；既有workflow、Provider与A/B多Thread证据所覆盖的生命周期没有被本次焦点局部改动扩写或重跑。

## Stop-loss

若出现Settings revision/settings错配、Web将accepted post-step failure误报为保存失败或重发mutation、后台Goal/Automation绕过surface policy、矩阵被复制到Web/Gateway/adapter/lifecycle、unknown/unsupported进入执行、Studio误吃Chat policy、per-turn Host热重载控制面、永久legacy双轨或第二settings/cache/registry，立即`SIMPLIFY`，不继续扩张。Web Access若出现Web/Curator独立Provider或logo清单、AgentGateway/第七组Host接入、整包availability布尔值、后台Provider健康daemon、全局Session registry/file watcher、运行时favicon/CDN热取、token进入Browser历史/restore、Curator复用当前Tab、后台Thread抢route/Right Dock、Curator暴露外部打开或raw token、Timeline假重开、为Provider字段建立通用form DSL、品牌资产缺少准确identity/source snapshot/hash记录或stock Pi兼容profile，同样立即停止。
