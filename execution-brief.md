# Execution brief

## 当前状态

当前唯一活动施工是 `codex/synara-073-adoption`：把 Synara adopted head 从 `57f48ef1` 推进到候选 `a93c47e2`，在既有 Product Orchestration、Desktop shutdown、RightDock Browser、Provider ingress 与 Git owner 内完成正常退出一次性恢复、Browser Tab、模型选择和性能改进。不得引入第二 Product Store、公共恢复 RPC、Floating Browser、第二 Browser runtime 或清空 bounded raw diagnostics。

主线是唯一工作基线。当前安装版、历史 DMG、测试计数、Provider probe 与 packaged journey 只在精确证据需要时从 Git 或对应 research owner 查找，不在本文件维护镜像。

## 下一动作

冻结并推送代码候选，随后只从该 exact pushed SHA 做隔离 packaged App 验证；验证闭合后更新唯一 adoption、architecture 与 research owner，并把本文件恢复成无活动状态。任何 drop、dup、reorder、silent fallback、重复恢复、错误目录或明显 UI 卡顿都停止 adopted revision 推进。

## Stop-loss

- 本文件只协调现在，不授予 source adoption、产品行为、发行或高风险动作。
- 稳定合同进入 `architecture/`；exact adoption进入 `source-adoptions.json`；固定反证进入 `research/`；claim状态进入active Campaign。
- 一项工作完成后删除其施工叙事，只保留仍真实存在的阻塞或下一动作。Git 历史承担历史，不建立第二总账。
