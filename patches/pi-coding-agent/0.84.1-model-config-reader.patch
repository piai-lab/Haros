diff --git a/package-lock.json b/package-lock.json
index 48e638d5e..c17a5db1e 100644
--- a/package-lock.json
+++ b/package-lock.json
@@ -3601,0 +3602,6 @@
+		"node_modules/jsonc-parser": {
+			"version": "3.3.1",
+			"resolved": "https://registry.npmjs.org/jsonc-parser/-/jsonc-parser-3.3.1.tgz",
+			"integrity": "sha512-HUgH65KyejrUFPvHFPbqOY0rsFip3Bo5wb4ngvdi1EpCYWUQDC5V+Y7mZws+DLkr4M//zQJoanu1SP+87Dv1oQ==",
+			"license": "MIT"
+		},
@@ -5557,0 +5564 @@
+				"jsonc-parser": "3.3.1",
diff --git a/packages/coding-agent/package.json b/packages/coding-agent/package.json
index f6fc21a3d..1e293a821 100644
--- a/packages/coding-agent/package.json
+++ b/packages/coding-agent/package.json
@@ -60,0 +61 @@
+		"jsonc-parser": "3.3.1",
diff --git a/packages/coding-agent/src/core/model-config.ts b/packages/coding-agent/src/core/model-config.ts
index 7f679a494..4a1a13597 100644
--- a/packages/coding-agent/src/core/model-config.ts
+++ b/packages/coding-agent/src/core/model-config.ts
@@ -3 +3,6 @@
-import { readFile } from "node:fs/promises";
+import { constants as fsConstants } from "node:fs";
+import { randomUUID } from "node:crypto";
+import { chmod, lstat, mkdir, open, readFile, realpath, rename, rm } from "node:fs/promises";
+import { basename, dirname, join } from "node:path";
+import { applyEdits, modify } from "jsonc-parser";
+import lockfile from "proper-lockfile";
@@ -215,0 +221,157 @@ type ModelsJson = Static<typeof ModelsConfigSchema>;
+export interface ModelConfigReadOptions {
+	readonly signal?: AbortSignal;
+}
+
+export type ModelConfigReader = (options: ModelConfigReadOptions) => Promise<string | undefined>;
+
+export type ModelConfigProviderMutation =
+	| {
+			type: "upsert";
+			providerId: string;
+			provider: ModelsJsonProvider;
+	  }
+	| {
+			type: "remove";
+			providerId: string;
+	  };
+
+export interface ModelConfigMutationOptions {
+	readonly signal?: AbortSignal;
+}
+
+export interface ModelConfigFileMutationOptions extends ModelConfigMutationOptions {
+	readonly validateCandidate: (candidatePath: string, signal?: AbortSignal) => Promise<void>;
+}
+
+export interface ModelConfigMutationResult {
+	readonly changed: boolean;
+	readonly providerIds: readonly string[];
+}
+
+const MAX_MODELS_CONFIG_BYTES = 4 * 1024 * 1024;
+const MODELS_CONFIG_FILE_MODE = 0o600;
+const MODEL_CONFIG_PROVIDER_FIELDS = [
+	"name",
+	"baseUrl",
+	"apiKey",
+	"api",
+	"oauth",
+	"headers",
+	"compat",
+	"authHeader",
+	"models",
+	"modelOverrides",
+] as const satisfies readonly (keyof ModelsJsonProvider)[];
+
+function mergeEditedModels(
+	currentModels: readonly ModelsJsonModel[] | undefined,
+	editedModels: readonly ModelsJsonModel[],
+): ModelsJsonModel[] {
+	const currentById = new Map((currentModels ?? []).map((model) => [model.id, model]));
+	return editedModels.map((editedModel) => {
+		const currentModel = currentById.get(editedModel.id);
+		return currentModel
+			? ({ ...structuredClone(currentModel), ...structuredClone(editedModel) } as ModelsJsonModel)
+			: structuredClone(editedModel);
+	});
+}
+
+/** Merge one credential-blind edit while retaining Pi-owned fields on matching models. */
+export function mergeModelConfigProviderEdit(
+	currentProvider: ModelsJsonProvider | undefined,
+	editedProvider: ModelsJsonProvider,
+): ModelsJsonProvider {
+	return {
+		...(currentProvider ? structuredClone(currentProvider) : {}),
+		...structuredClone(editedProvider),
+		...(editedProvider.models
+			? { models: mergeEditedModels(currentProvider?.models, editedProvider.models) }
+			: {}),
+	};
+}
+
+function isMissingPathError(error: unknown): boolean {
+	return typeof error === "object" && error !== null && "code" in error && error.code === "ENOENT";
+}
+
+function formattingOptions(content: string): {
+	insertSpaces: boolean;
+	tabSize: number;
+	eol: string;
+} {
+	const indentation = content.match(/(?:^|\r?\n)([ \t]+)"providers"\s*:/)?.[1] ?? "\t";
+	return {
+		insertSpaces: !indentation.includes("\t"),
+		tabSize: indentation.includes("\t") ? 4 : Math.max(1, indentation.length),
+		eol: content.includes("\r\n") ? "\r\n" : "\n",
+	};
+}
+
+type ModelConfigFileSnapshot = {
+	readonly content: string | undefined;
+	readonly identity:
+		| {
+				readonly dev: number;
+				readonly ino: number;
+				readonly size: number;
+				readonly mtimeMs: number;
+		  }
+		| undefined;
+};
+
+async function readModelConfigFile(modelsJsonPath: string, signal?: AbortSignal): Promise<ModelConfigFileSnapshot> {
+	signal?.throwIfAborted();
+	const flags = fsConstants.O_RDONLY | (fsConstants.O_NOFOLLOW ?? 0);
+	let handle: Awaited<ReturnType<typeof open>>;
+	try {
+		handle = await open(modelsJsonPath, flags);
+	} catch (error) {
+		if (isMissingPathError(error)) return { content: undefined, identity: undefined };
+		throw error;
+	}
+	try {
+		const metadata = await handle.stat();
+		if (!metadata.isFile() || metadata.nlink !== 1 || metadata.size > MAX_MODELS_CONFIG_BYTES) {
+			throw new Error("models.json is not a private regular file or exceeds the safe mutation boundary");
+		}
+		const bytes = await handle.readFile();
+		signal?.throwIfAborted();
+		return {
+			content: new TextDecoder("utf-8", { fatal: true }).decode(bytes),
+			identity: {
+				dev: metadata.dev,
+				ino: metadata.ino,
+				size: metadata.size,
+				mtimeMs: metadata.mtimeMs,
+			},
+		};
+	} finally {
+		await handle.close();
+	}
+}
+
+async function assertModelConfigUnchanged(
+	modelsJsonPath: string,
+	identity: ModelConfigFileSnapshot["identity"],
+): Promise<void> {
+	try {
+		const current = await lstat(modelsJsonPath);
+		if (
+			!identity ||
+			current.isSymbolicLink() ||
+			!current.isFile() ||
+			current.nlink !== 1 ||
+			current.dev !== identity.dev ||
+			current.ino !== identity.ino ||
+			current.size !== identity.size ||
+			current.mtimeMs !== identity.mtimeMs
+		) {
+			throw new Error("models.json changed during the persistent mutation");
+		}
+	} catch (error) {
+		if (isMissingPathError(error) && !identity) return;
+		if (isMissingPathError(error)) throw new Error("models.json changed during the persistent mutation");
+		throw error;
+	}
+}
+
@@ -245,14 +407 @@ export class ModelConfig {
-	static async load(modelsJsonPath: string | undefined): Promise<ModelConfig> {
-		if (!modelsJsonPath) return new ModelConfig(new Map());
-		const path = normalizePath(modelsJsonPath);
-		let content: string;
-		try {
-			content = await readFile(path, "utf-8");
-		} catch (error) {
-			if ((error as NodeJS.ErrnoException).code === "ENOENT") return new ModelConfig(new Map());
-			return new ModelConfig(
-				new Map(),
-				`Failed to load models.json: ${error instanceof Error ? error.message : error}\n\nFile: ${path}`,
-			);
-		}
-
+	private static fromContent(content: string, sourceLabel?: string): ModelConfig {
@@ -265 +414 @@ export class ModelConfig {
-				`Failed to parse models.json: ${error instanceof Error ? error.message : error}\n\nFile: ${path}`,
+				`Failed to parse models.json: ${error instanceof Error ? error.message : error}${sourceLabel ? `\n\nFile: ${sourceLabel}` : ""}`,
@@ -275 +424,4 @@ export class ModelConfig {
-			return new ModelConfig(new Map(), `Invalid models.json schema:\n${errors}\n\nFile: ${path}`);
+			return new ModelConfig(
+				new Map(),
+				`Invalid models.json schema:\n${errors}${sourceLabel ? `\n\nFile: ${sourceLabel}` : ""}`,
+			);
@@ -285,0 +438,35 @@ export class ModelConfig {
+	static async load(
+		modelsJsonPath: string | undefined,
+		options: { reader?: ModelConfigReader; signal?: AbortSignal } = {},
+	): Promise<ModelConfig> {
+		options.signal?.throwIfAborted();
+		let content: string | undefined;
+		let normalizedPath: string | undefined;
+		if (options.reader) {
+			try {
+				content = await options.reader({ signal: options.signal });
+				options.signal?.throwIfAborted();
+			} catch {
+				options.signal?.throwIfAborted();
+				return new ModelConfig(new Map(), "Failed to load models.json from configured reader");
+			}
+		} else {
+			if (!modelsJsonPath) return new ModelConfig(new Map());
+			normalizedPath = normalizePath(modelsJsonPath);
+			try {
+				content = await readFile(normalizedPath, "utf-8");
+			} catch (error) {
+				if ((error as NodeJS.ErrnoException).code === "ENOENT") return new ModelConfig(new Map());
+				return new ModelConfig(
+					new Map(),
+					`Failed to load models.json: ${error instanceof Error ? error.message : error}\n\nFile: ${normalizedPath}`,
+				);
+			}
+		}
+		if (content === undefined) return new ModelConfig(new Map());
+		options.signal?.throwIfAborted();
+
+		options.signal?.throwIfAborted();
+		return ModelConfig.fromContent(content, normalizedPath);
+	}
+
@@ -289,0 +477,6 @@ export class ModelConfig {
+	withProviderEdit(providerId: string, provider: ModelsJsonProvider): ModelConfig {
+		const providers = new Map(this.providers);
+		providers.set(providerId, deepFreeze(mergeModelConfigProviderEdit(this.providers.get(providerId), provider)));
+		return new ModelConfig(providers, this.error);
+	}
+
@@ -296,0 +490,117 @@ export class ModelConfig {
+
+	static validateContent(content: string): ModelConfig {
+		return ModelConfig.fromContent(content);
+	}
+}
+
+/**
+ * Persist one typed provider mutation while preserving unrelated JSONC bytes.
+ * The explicit file path is used only for this user-requested mutation; passive
+ * ModelRuntime reads continue to use the caller-owned content reader.
+ */
+export async function mutateModelConfigProviderFile(
+	modelsJsonPath: string,
+	mutation: ModelConfigProviderMutation,
+	options: ModelConfigFileMutationOptions,
+): Promise<ModelConfigMutationResult> {
+	options.signal?.throwIfAborted();
+	if (!mutation.providerId.trim() || mutation.providerId !== mutation.providerId.trim()) {
+		throw new Error("Model provider id must not be empty or padded");
+	}
+	const requestedPath = normalizePath(modelsJsonPath);
+	const parentDir = dirname(requestedPath);
+	await mkdir(parentDir, { recursive: true, mode: 0o700 });
+	const parentMetadata = await lstat(parentDir);
+	if (parentMetadata.isSymbolicLink() || !parentMetadata.isDirectory()) {
+		throw new Error("models.json parent is not a private directory");
+	}
+	const normalizedPath = join(await realpath(parentDir), basename(requestedPath));
+
+	let compromised: Error | undefined;
+	const release = await lockfile.lock(normalizedPath, {
+		realpath: false,
+		retries: { retries: 10, factor: 1.5, minTimeout: 10, maxTimeout: 100 },
+		stale: 30_000,
+		onCompromised: (error) => {
+			compromised = error;
+		},
+	});
+	let temporaryPath: string | undefined;
+	try {
+		options.signal?.throwIfAborted();
+		if (compromised) throw compromised;
+		const current = await readModelConfigFile(normalizedPath, options.signal);
+		const currentContent = current.content ?? '{\n\t"providers": {}\n}\n';
+		const currentConfig = ModelConfig.validateContent(currentContent);
+		if (currentConfig.getError()) throw new Error(currentConfig.getError());
+		if (mutation.type === "remove" && !currentConfig.getProvider(mutation.providerId)) {
+			return { changed: false, providerIds: currentConfig.getProviderIds() };
+		}
+
+		let nextContent = currentContent;
+		if (mutation.type === "remove") {
+			nextContent = applyEdits(
+				nextContent,
+				modify(nextContent, ["providers", mutation.providerId], undefined, {
+					formattingOptions: formattingOptions(nextContent),
+				}),
+			);
+		} else if (!currentConfig.getProvider(mutation.providerId)) {
+			nextContent = applyEdits(
+				nextContent,
+				modify(nextContent, ["providers", mutation.providerId], mutation.provider, {
+					formattingOptions: formattingOptions(nextContent),
+				}),
+			);
+		} else {
+			const currentProvider = currentConfig.getProvider(mutation.providerId);
+			const mergedProvider = mergeModelConfigProviderEdit(currentProvider, mutation.provider);
+			for (const field of MODEL_CONFIG_PROVIDER_FIELDS) {
+				if (!Object.hasOwn(mutation.provider, field)) continue;
+				nextContent = applyEdits(
+					nextContent,
+					modify(nextContent, ["providers", mutation.providerId, field], mergedProvider[field], {
+						formattingOptions: formattingOptions(nextContent),
+					}),
+				);
+			}
+		}
+		if (Buffer.byteLength(nextContent, "utf8") > MAX_MODELS_CONFIG_BYTES) {
+			throw new Error("models.json exceeds the safe mutation boundary");
+		}
+		const nextConfig = ModelConfig.validateContent(nextContent);
+		if (nextConfig.getError()) throw new Error(nextConfig.getError());
+		if (mutation.type === "upsert" && !nextConfig.getProvider(mutation.providerId)) {
+			throw new Error("Model provider mutation was not accepted by the official schema");
+		}
+		if (mutation.type === "remove" && nextConfig.getProvider(mutation.providerId)) {
+			throw new Error("Model provider removal was not accepted by the official schema");
+		}
+		if (nextContent === currentContent) {
+			return { changed: false, providerIds: nextConfig.getProviderIds() };
+		}
+
+		options.signal?.throwIfAborted();
+		if (compromised) throw compromised;
+		await assertModelConfigUnchanged(normalizedPath, current.identity);
+		temporaryPath = `${normalizedPath}.${randomUUID()}.tmp`;
+		const flags = fsConstants.O_CREAT | fsConstants.O_EXCL | fsConstants.O_WRONLY | (fsConstants.O_NOFOLLOW ?? 0);
+		const handle = await open(temporaryPath, flags, MODELS_CONFIG_FILE_MODE);
+		try {
+			await handle.writeFile(nextContent, "utf8");
+			await handle.sync();
+		} finally {
+			await handle.close();
+		}
+		await options.validateCandidate(temporaryPath, options.signal);
+		options.signal?.throwIfAborted();
+		if (compromised) throw compromised;
+		await assertModelConfigUnchanged(normalizedPath, current.identity);
+		await rename(temporaryPath, normalizedPath);
+		temporaryPath = undefined;
+		await chmod(normalizedPath, MODELS_CONFIG_FILE_MODE);
+		return { changed: true, providerIds: nextConfig.getProviderIds() };
+	} finally {
+		if (temporaryPath) await rm(temporaryPath, { force: true });
+		await release().catch(() => undefined);
+	}
diff --git a/packages/coding-agent/src/core/model-runtime.ts b/packages/coding-agent/src/core/model-runtime.ts
index 6f4071b60..5746f6c2b 100644
--- a/packages/coding-agent/src/core/model-runtime.ts
+++ b/packages/coding-agent/src/core/model-runtime.ts
@@ -15,0 +16 @@ import {
+	InMemoryCredentialStore,
@@ -43 +44,9 @@ import { AuthStorage as DefaultAuthStorage } from "./auth-storage.ts";
-import { ModelConfig } from "./model-config.ts";
+import {
+	ModelConfig,
+	mutateModelConfigProviderFile,
+	type ModelConfigMutationOptions,
+	type ModelConfigMutationResult,
+	type ModelConfigProviderMutation,
+	type ModelConfigReader,
+	type ModelsJsonProvider,
+} from "./model-config.ts";
@@ -57,0 +67,29 @@ import { RuntimeCredentials } from "./runtime-credentials.ts";
+export type { ModelConfigReader, ModelConfigReadOptions } from "./model-config.ts";
+
+/**
+ * Persist one provider only after a fresh official runtime can reload and
+ * compose the complete candidate file. The candidate uses memory-only auth and
+ * catalog stores, so validation cannot read or mutate the user's credentials.
+ */
+export async function mutateModelConfigProvider(
+	modelsJsonPath: string,
+	mutation: ModelConfigProviderMutation,
+	options: ModelConfigMutationOptions = {},
+): Promise<ModelConfigMutationResult> {
+	return mutateModelConfigProviderFile(modelsJsonPath, mutation, {
+		...options,
+		validateCandidate: async (candidatePath, signal) => {
+			const runtime = await ModelRuntime.create({
+				credentials: new InMemoryCredentialStore(),
+				modelsPath: candidatePath,
+				modelsStore: new InMemoryCodingAgentModelsStore(),
+				allowModelNetwork: false,
+				refreshOnCreate: false,
+				signal,
+			});
+			const error = runtime.getError();
+			if (error) throw new Error(error);
+		},
+	});
+}
+
@@ -70,0 +109,2 @@ export interface CreateModelRuntimeOptions {
+	/** Read models.json content through a caller-owned safety boundary. Requires modelsPath: null. */
+	modelsConfigReader?: ModelConfigReader;
@@ -138,0 +179 @@ export class ModelRuntime implements Models {
+	private readonly modelsConfigReader: ModelConfigReader | undefined;
@@ -157,0 +199 @@ export class ModelRuntime implements Models {
+		modelsConfigReader: ModelConfigReader | undefined,
@@ -164,0 +207 @@ export class ModelRuntime implements Models {
+		this.modelsConfigReader = modelsConfigReader;
@@ -172,0 +216,3 @@ export class ModelRuntime implements Models {
+		if (options.modelsConfigReader && options.modelsPath !== null) {
+			throw new Error("modelsConfigReader requires modelsPath: null");
+		}
@@ -176 +222,4 @@ export class ModelRuntime implements Models {
-		const config = await ModelConfig.load(modelsPath);
+		const config = await ModelConfig.load(modelsPath, {
+			reader: options.modelsConfigReader,
+			signal: options.signal,
+		});
@@ -179,3 +228,5 @@ export class ModelRuntime implements Models {
-			(modelsPath
-				? new FileModelsStore(options.modelsStorePath ?? join(dirname(modelsPath), "models-store.json"))
-				: new InMemoryCodingAgentModelsStore());
+			(options.modelsStorePath
+				? new FileModelsStore(options.modelsStorePath)
+				: modelsPath
+					? new FileModelsStore(join(dirname(modelsPath), "models-store.json"))
+					: new InMemoryCodingAgentModelsStore());
@@ -193,0 +245 @@ export class ModelRuntime implements Models {
+			options.modelsConfigReader,
@@ -572,0 +625,28 @@ export class ModelRuntime implements Models {
+	/** Provider IDs accepted from the current official models.json configuration snapshot. */
+	getModelConfigProviderIds(): readonly string[] {
+		return this.config.getProviderIds();
+	}
+
+	/** Credential-blind provider configuration accepted by ModelConfig. */
+	getModelConfigProvider(providerId: string): ModelsJsonProvider | undefined {
+		const provider = this.config.getProvider(providerId);
+		if (!provider) return undefined;
+		const credentialBlindClone = (value: unknown): unknown => {
+			if (Array.isArray(value)) return value.map(credentialBlindClone);
+			if (typeof value !== "object" || value === null) return value;
+			return Object.fromEntries(
+				Object.entries(value)
+					.filter(([key]) => key !== "apiKey" && key !== "headers")
+					.map(([key, child]) => [key, credentialBlindClone(child)]),
+			);
+		};
+		return credentialBlindClone(provider) as ModelsJsonProvider;
+	}
+
+	/** Apply one credential-blind edit to this disposable runtime without exposing retained secrets. */
+	registerModelConfigProviderPreview(providerId: string, provider: ModelsJsonProvider): void {
+		this.config = this.config.withProviderEdit(providerId, provider);
+		this.recomposeProvider(providerId);
+		this.updateModelSnapshot();
+	}
+
@@ -691 +771,4 @@ export class ModelRuntime implements Models {
-		this.config = await ModelConfig.load(this.modelsPath);
+		this.config = await ModelConfig.load(this.modelsPath, {
+			reader: this.modelsConfigReader,
+			signal: options.signal,
+		});
diff --git a/packages/coding-agent/src/core/provider-composer.ts b/packages/coding-agent/src/core/provider-composer.ts
index 1be7fcdb5..04b8931c2 100644
--- a/packages/coding-agent/src/core/provider-composer.ts
+++ b/packages/coding-agent/src/core/provider-composer.ts
@@ -447,2 +447,10 @@ export function composeModelProvider(
-	// Validate eagerly so registration/reload reports structural errors immediately.
-	getModels();
+	// Validate eagerly so registration/reload reports structural and execution
+	// protocol errors before a provider becomes persistent or selectable.
+	const initialModels = getModels();
+	for (const model of initialModels) {
+		const extensionOwnsApi = extension?.streamSimple !== undefined && model.api === extension.api;
+		const baseOwnsApi = base?.getModels().some((entry) => entry.api === model.api) ?? false;
+		if (!extensionOwnsApi && !baseOwnsApi && !getApiProvider(model.api)) {
+			throw new Error(`No API provider registered for api: ${model.api}`);
+		}
+	}
diff --git a/packages/coding-agent/src/index.ts b/packages/coding-agent/src/index.ts
index 3d05c2af4..a7df13424 100644
--- a/packages/coding-agent/src/index.ts
+++ b/packages/coding-agent/src/index.ts
@@ -171,0 +172,8 @@ export { ModelRegistry } from "./core/model-registry.ts";
+export {
+	type ModelConfigMutationOptions,
+	type ModelConfigMutationResult,
+	type ModelConfigProviderMutation,
+	type ModelsJsonModel,
+	type ModelsJsonModelOverride,
+	type ModelsJsonProvider,
+} from "./core/model-config.ts";
@@ -180,0 +189,3 @@ export {
+	mutateModelConfigProvider,
+	type ModelConfigReader,
+	type ModelConfigReadOptions,
