{
  "extendsFixture": "product-refusal-lock-positive",
  "textReplacements": {
    "apps/service/src/product/ProductControlPlane.ts": [[
      "  const lock = await acquireDatabaseLifecycleLock(databasePath);",
      "  const lock = await acquireDatabaseLifecycleLock(databasePath);\n  const captured = { release: () => releaseDatabaseLifecycleLock(lock) };\n  Promise.resolve().then(() => captured.release());\n  await Promise.resolve();"
    ]]
  }
}
