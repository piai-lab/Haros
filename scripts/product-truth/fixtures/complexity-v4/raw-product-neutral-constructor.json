{
  "extendsFixture": "product-refusal-lock-positive",
  "textReplacements": {
    "apps/service/src/product/ProductControlPlane.ts": [
      ["    const database = new DatabaseSync(databasePath);", "    const location = join(stateDir, \"stores\", \"product.sqlite\");\n    const database = new DatabaseSync(location);"]
    ]
  }
}
