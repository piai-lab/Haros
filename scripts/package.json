{
  "name": "@harnessos/scripts",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "effect-language-service patch",
    "claude-fast-mode-probe": "bun run claude-fast-mode-probe.ts",
    "claude-haiku-thinking-probe": "bun run claude-haiku-thinking-probe.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run",
    "test:related": "vitest related --passWithNoTests"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.207",
    "@effect/platform-node": "catalog:",
    "@harnessos/contracts": "workspace:*",
    "@harnessos/shared": "workspace:*",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@effect/language-service": "catalog:",
    "@effect/vitest": "catalog:",
    "@electron/asar": "3.4.1",
    "@types/bun": "catalog:",
    "electron-builder": "26.15.3",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
