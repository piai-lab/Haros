# HarnessOS Foundation Goal

## 目标

在 `/Users/liuzaoqu/Desktop/Develop/independent/HarnessOS` 将锁定的 OmniMind alpha 基线收口为独立、干净、可公开的 HarnessOS 范本仓库：以 OA 为默认 Pi-derived Engine，保留独立 stock Pi，完整保全 Agent / Chat / Studio 与单一 Product Orchestration，并通过源码、法定、UI、状态隔离和 unsigned packaged 验收。

## 范围与边界

纳入仓库切割、HarnessOS/OA 身份、`Provider* → Engine*`、`HostGateway`、OA/Pi 隔离、Pi-family 与 UI owner cut、Apache-2.0 与 notices、双语和选定品牌、工程/packaged 证明及 GitHub 公开门。授权在当前任务分支编辑、重命名、测试、构建、生成 unsigned artifact、commit/push，并在全部公开门通过后配置精确 GitHub 仓库。排除来源升级、旧用户数据迁移、共享核心/同步、动态 Engine 插件平台、签名/公证/Release/updater feed。停止边界：不读、不迁移、不删除 `.omnimind`；不建 alias、dual-read 或 fallback；不在未选 visual master 时集成品牌几何；不在公开门未通过时转 public。

## 零记忆启动

你处于 fresh、零记忆会话，不得依赖聊天回忆。任何写操作前：

1. 解析精确工作区、Git 仓库/分支/HEAD、适用指令和当前 dirty paths；
2. 按 `missions/harnessos-foundation.md` 的 Core Read Order 读完全部 core evidence；
3. 原样运行该 spec 的唯一 `Bootstrap command`，与 Last reconciled revision、worktree state 和 active Claim 对账；
4. 在当前上下文给出一次 Orientation Receipt：已解析根目录/版本、权威 spec、active Claim、关键不变量与证据缺口；不落盘；
5. 路径缺失、证据不可读、版本失配或核心事实未知时，先更新 spec 对账或将受影响 Claim 标为 blocked，不得猜测实施。

第一安全动作：针对 active Claim C-002，从 package/app 用户入口追到唯一 identity writer、构建消费者、failure/restart/shutdown 和 absence falsifier，不先做全局名称替换。

## 执行协议

当前 active Claim 为 C-002。只实施 spec 中 active/受影响 Claim；每个 owner cut 先 characterization，再切换全部 consumer，删除旧 truth/writer/fallback/tests/docs residue，不留长期双轨。每次完整关注点做最窄 focused proof，将原始结果按固定 receipt 格式落盘，更新 spec 后再 commit/push 当前任务分支。新增一个 fixture Engine 的半径演练不得触达 ChatView/Sidebar 清单，删除后不得留 registry/Settings/asset residue。版本/工作树漂移时先更新 spec。无需独立验收且不并行的机械步骤不创建 subagent。

## 验收与完成权

当前 active Claim C-002 见 spec；Focused proof 必须能推翻它。冻结候选后，在同一 exact SHA 原样运行 spec 的唯一 `Final gate command`，并将 macOS isolated packaged journey 与 Linux/Windows unsigned startup 证据绑定同一 SHA。仅当所有 `[required]` Claims 在同一最终版本由真实 fresh independent evaluator 完成 fresh completion audit 验证、final gate 通过、blocked=0 且 audit 无 material finding 时才能 DONE。生产者只能提交 candidate；当前上下文不授权独立 evaluator，因此不得自行写 verified。发布、签名、Release、feed、历史改写、用户数据处置或未解决 visual master 都是停止条件。

详见：`missions/harnessos-foundation.md`
